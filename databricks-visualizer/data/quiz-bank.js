// Per-module quiz bank. A "Test Yourself" section auto-appears on any module
// whose id has an entry here. Each: { q, options, answer:<0-based>, explanation }.

export const QUIZ_BANK = {
  m01: [
    { q: 'What does Databricks primarily add on top of vanilla Apache Spark on EMR?',
      options: ['Faster raw CPUs', 'A managed control plane — Delta Lake, Unity Catalog, MLflow, Photon, managed clusters', 'A different query language', 'Cheaper object storage'],
      answer: 1,
      explanation: 'Databricks is Spark plus a managed platform: collaborative notebooks, one-click clusters, built-in Delta Lake (ACID), Unity Catalog (governance), MLflow, and the Photon runtime — all of which you would build and operate yourself on raw EMR.' },
    { q: 'Why does the Lakehouse cut storage cost versus a lake + warehouse?',
      options: ['It compresses data 100×', 'It keeps one copy of data on cheap object storage instead of duplicating it into a warehouse', 'It deletes old data automatically', 'It avoids using Parquet'],
      answer: 1,
      explanation: 'A two-tier design stores data twice and needs ETL to move it. The Lakehouse keeps a single copy as Delta tables on object storage (~10× cheaper than warehouse storage) that every workload reads directly.' },
    { q: 'How does Delta Lake provide ACID guarantees on S3?',
      options: ['An external lock server', 'A transaction log of JSON commits plus optimistic concurrency control', 'By disabling concurrent writers', 'Row-level locking in Parquet'],
      answer: 1,
      explanation: 'Delta appends a JSON commit to _delta_log for every write; readers replay the log to find valid files. Conflicts are caught by optimistic concurrency (one writer wins the version, the other retries) — no external lock needed.' },
  ],
  m02: [
    { q: 'What does the Lakehouse eliminate compared with a Lambda architecture?',
      options: ['The need for object storage', 'The separate batch and speed codepaths kept in sync', 'SQL support', 'Schema enforcement'],
      answer: 1,
      explanation: 'Lambda maintains two codepaths (batch + speed). The Lakehouse serves batch and streaming from the same Delta tables through one engine, with no reprocessing and no duplicated business logic.' },
    { q: 'After how many commits does Delta write a Parquet checkpoint to speed up log replay?',
      options: ['2', '10', '100', '1000'],
      answer: 1,
      explanation: 'Every 10 commits Delta writes a checkpoint capturing the full current state, so readers replay from the latest checkpoint plus a few commits instead of the entire history.' },
    { q: 'What turns a plain directory of Parquet files into a managed, ACID table?',
      options: ['The Hive metastore', 'The _delta_log transaction log', 'A SQL Warehouse', 'Unity Catalog alone'],
      answer: 1,
      explanation: 'The _delta_log directory of JSON commits (which files are added/removed, schema, stats) is what provides ACID, time travel, and audit history over otherwise-plain Parquet files.' },
  ],
  m03: [
    { q: 'During MERGE INTO, how does Delta avoid reading every data file?',
      options: ['It reads them all in parallel', 'It uses per-file min/max statistics in the log to pick only candidate files', 'It caches the whole table in memory', 'It rewrites the table first'],
      answer: 1,
      explanation: 'Delta checks the min/max column statistics recorded in the log and reads only files whose ranges overlap the join condition — data skipping that can cut I/O dramatically.' },
    { q: 'You VACUUM a table, then try to time-travel to a version older than the retention window. What happens?',
      options: ['It succeeds normally', 'It returns empty results', 'It fails with FileNotFoundException', 'It rebuilds the old files'],
      answer: 2,
      explanation: 'VACUUM physically deletes Parquet files past the retention period (default 7 days). Time travel still finds the log entry but the underlying files are gone, so it fails with FileNotFoundException.' },
    { q: 'Under optimistic concurrency, two INSERT-only writers append to different partitions at the same time. Result?',
      options: ['Both fail', 'One is silently dropped', 'Both succeed — the writes are compatible', 'The table is corrupted'],
      answer: 2,
      explanation: 'Non-overlapping writes (e.g. INSERTs to different partitions/files) are compatible: the losing writer re-reads the log, sees no conflict with its files, and commits successfully. Conflicts only arise on overlapping file ranges.' },
  ],
  m04: [
    { q: 'Which cluster type should run a nightly 30-minute ETL job?',
      options: ['All-purpose cluster', 'Job cluster (ephemeral)', 'SQL Warehouse', 'A single-node cluster'],
      answer: 1,
      explanation: 'A job cluster is created for the run and terminated on completion, so you pay only for the 30 minutes. An all-purpose cluster bills idle DBUs the rest of the day.' },
    { q: 'A job slowed from 30 min to 3 hours. What is the first thing to inspect?',
      options: ['The billing dashboard', 'Spark UI → Stages for the longest stage and data skew', 'The notebook comments', 'The cluster name'],
      answer: 1,
      explanation: 'Start in the Spark UI: find the longest stage, then check for data skew, disk spill, small-file explosion, or a bad join — the usual causes of a sudden slowdown.' },
    { q: 'Which runtime should you pick for an ML training workload?',
      options: ['Standard DBR', 'DBR ML', 'A SQL Warehouse', 'Photon-only cluster'],
      answer: 1,
      explanation: 'DBR ML ships scikit-learn, TensorFlow, PyTorch, XGBoost, Hugging Face, MLflow, and GPU drivers pre-configured — faster and less error-prone than installing them onto standard DBR yourself.' },
  ],
};
