/* ============================================================
   Interview Bank — curated, per-module interview questions.
   Keyed by module id. Each: { q, a, difficulty }.
   When a module appears here, it is the authoritative source
   for that module's interview section (enhance.js prefers it
   over the legacy architectureLayers / categorized bank).
   ============================================================ */

(function () {
  'use strict';

  const InterviewBank = {
    intro: [
      { q: 'What problem does Snowflake solve that traditional data warehouses did not?', a: 'Traditional warehouses (Teradata, on-prem Oracle, early Redshift) couple storage and compute, so you must over-provision expensive compute just to keep data online, and heavy jobs starve other users. Snowflake decouples them: one shared copy of data in cloud storage, many independent compute clusters, each billed per second — solving cost, concurrency, and scaling in one architecture.', difficulty: 'beginner' },
      { q: 'What does "separation of storage and compute" actually mean?', a: 'Data lives in cloud object storage (S3/Blob/GCS) as compressed columnar micro-partitions, completely independent of the machines that run queries. Virtual warehouses are ephemeral compute clusters that read that shared data on demand. You can resize or suspend compute without touching data, and run many warehouses against the same data at once.', difficulty: 'beginner' },
      { q: 'Why is Snowflake called "cloud-native" rather than just cloud-hosted?', a: 'It was built from scratch for the cloud rather than lifting an existing database onto cloud VMs. It uses object storage as its storage tier, elastic stateless compute, and a multi-tenant services layer — none of which a ported on-prem engine has. That design is what enables per-second elasticity, instant scaling, and zero-copy sharing.', difficulty: 'intermediate' },
      { q: 'How does per-second billing change cost behavior versus provisioned clusters?', a: 'You pay only while a warehouse runs, and it auto-suspends when idle, so bursty workloads cost far less than a 24/7 provisioned cluster sized for peak. The tradeoff is you must set sensible auto-suspend and monitor credits — idle-but-not-suspended warehouses and oversized warehouses are the main ways teams overspend.', difficulty: 'intermediate' },
      { q: 'Why can Netflix give every team its own warehouse without duplicating data?', a: 'All warehouses read the same micro-partitions in shared storage; a warehouse is just compute. So ML_TRAINING_WH, ANALYTICS_WH, and EXEC_WH each get isolated performance while querying one copy of the data. Isolation comes from separate compute, not separate data.', difficulty: 'intermediate' },
      { q: 'When might Snowflake NOT be the right choice?', a: 'For high-volume low-latency OLTP (single-row lookups/updates), heavy unstructured/streaming ML on raw files better suited to a lakehouse, or tiny always-on workloads where a small managed Postgres is cheaper. Snowflake shines at analytical, multi-workload SQL — not transactional systems of record.', difficulty: 'advanced' },
    ],
    'query-execution': [
      { q: 'Walk through the lifecycle of a query from submission to result.', a: 'Cloud Services authenticates and authorizes the session, parses and compiles the SQL, and builds a cost-based plan using metadata (pruning micro-partitions). It checks the result cache — a hit returns instantly. Otherwise the compiled plan runs on the target virtual warehouse, which reads needed micro-partitions (from local SSD cache or storage), executes joins/aggregations, and streams results back.', difficulty: 'intermediate' },
      { q: 'Where does compilation happen versus execution?', a: 'Compilation and optimization happen centrally in the Cloud Services layer, independent of any warehouse — that is why metadata-only operations need no running warehouse. Execution of the compiled plan happens on the virtual warehouse\'s compute nodes, which is the only part that consumes warehouse credits.', difficulty: 'intermediate' },
      { q: 'How does the optimizer decide things like join order without indexes?', a: 'It is cost-based and uses automatically-collected statistics per micro-partition (row counts, min/max, distinct values) rather than user indexes. It estimates the cheapest join order and pruning opportunities from those stats. You influence it mainly by writing selective filters and, on huge tables, clustering — not by hand-tuning indexes.', difficulty: 'advanced' },
      { q: 'What causes a query to "spill," and how do you fix it?', a: 'Spilling means an operation (large sort, join, or aggregation) exceeded the warehouse\'s memory and had to write to local — then remote — storage, which is slow. The Query Profile shows "bytes spilled." Fixes: size the warehouse up (more memory), reduce data scanned with better filters/pruning, or lower join cardinality before the heavy step.', difficulty: 'advanced' },
      { q: 'How does the result cache short-circuit execution?', a: 'If an identical query runs within 24 hours, the underlying data is unchanged, and the role has access, Cloud Services returns the stored result without using any warehouse. It is invalidated by DML on the referenced tables and by non-deterministic functions like CURRENT_TIMESTAMP.', difficulty: 'beginner' },
      { q: 'What do you look at first in a slow Query Profile?', a: 'The most expensive operator by percentage, then three tells: high "bytes scanned" (poor pruning — improve filters/clustering), "bytes spilled" (warehouse too small), and exploding row counts across a join (bad join or missing filter). Fix the dominant cost first rather than blindly resizing.', difficulty: 'intermediate' },
    ],
    'semi-structured': [
      { q: 'What is the VARIANT type and when do you use it?', a: 'VARIANT is a universal column type that stores any semi-structured value — JSON objects, arrays, or scalars — in Snowflake\'s optimized internal format. Use it to land data whose schema is unknown, nested, or evolving (event logs, API payloads) without defining columns up front, then query it with SQL.', difficulty: 'beginner' },
      { q: 'How do you extract a nested value from a VARIANT column?', a: 'Use path notation: colon to walk keys (v:user_id), dot for nested objects (v:device.os), and brackets for array elements (v:titles[0]). Values are untyped until cast, so append ::TYPE, e.g. v:user_id::NUMBER. Casting matters for correct comparisons and joins.', difficulty: 'beginner' },
      { q: 'What does LATERAL FLATTEN do?', a: 'It explodes an array or object inside a VARIANT into one row per element, so you can turn a nested array into a relational result. You reference each element via the function\'s output (f.value, f.index, f.key). It is the standard way to normalize repeated nested data for joins and aggregation.', difficulty: 'intermediate' },
      { q: 'How does Snowflake query JSON efficiently without a fixed schema?', a: 'On ingest it shreds VARIANT data into a columnar representation and collects per-path statistics, so path lookups and pruning work much like on typed columns. You get schema-on-read flexibility with close-to-columnar performance — though very heavy, repeated access can still benefit from flattening into typed columns.', difficulty: 'intermediate' },
      { q: 'When would you flatten-and-persist versus query VARIANT directly?', a: 'Query VARIANT directly for exploration or low-frequency access. Flatten into typed columns (a table or Dynamic Table) when the same nested fields are read constantly by dashboards or joins — typed columns give better compression, simpler SQL, and let the optimizer prune and cluster on those fields.', difficulty: 'advanced' },
      { q: 'How do you load a JSON file that is one large array of records?', a: 'Use a JSON file format with STRIP_OUTER_ARRAY = TRUE in COPY INTO, which loads each array element as its own row in a VARIANT column instead of one giant row. From there you cast/flatten the paths you need.', difficulty: 'intermediate' },
    ],
    objects: [
      { q: 'Describe the Snowflake object hierarchy.', a: 'Organization → Account → Database → Schema → object (tables, views, stages, etc.). Warehouses, roles, users, and resource monitors are account-level objects that sit alongside databases. Names resolve as DATABASE.SCHEMA.OBJECT, and roles are granted privileges at each level.', difficulty: 'beginner' },
      { q: 'What is the difference between a database and a schema?', a: 'A database is a top-level container for data; a schema is a namespace within a database that groups related tables, views, and other objects. Schemas let you separate concerns (e.g. RAW, STAGING, ANALYTICS) and grant access at a finer grain than the whole database.', difficulty: 'beginner' },
      { q: 'Compare permanent, transient, and temporary tables.', a: 'Permanent tables have full Time Travel (up to 90 days) and Fail-Safe. Transient tables persist across sessions but have no Fail-Safe and max 1-day Time Travel — cheaper for reproducible/staging data. Temporary tables exist only for the session and vanish on disconnect. You trade data-protection for storage cost.', difficulty: 'intermediate' },
      { q: 'What are the tradeoffs of transient tables?', a: 'They cut storage cost by removing the 7-day Fail-Safe and limiting Time Travel to 0–1 day, which is ideal for ETL staging or easily-rebuilt data. The risk: no last-resort recovery, so never use them for the only copy of important data.', difficulty: 'intermediate' },
      { q: 'How do stages, file formats, and pipes relate?', a: 'A stage points at files (internal or an external bucket); a file format describes how to parse them (JSON/CSV/Parquet…); COPY INTO loads staged files using a format; and a pipe wraps a COPY statement so Snowpipe can auto-load files as they arrive. They are the building-block objects of ingestion.', difficulty: 'intermediate' },
      { q: 'What is the difference between a view, a materialized view, and a secure view?', a: 'A standard view is a stored query executed on read (always fresh, no storage). A materialized view pre-computes and auto-maintains results for fast repeated reads (Enterprise; storage + maintenance cost). A secure view hides its definition and bypasses certain optimizations to prevent data inference — used when sharing sensitive logic/data.', difficulty: 'advanced' },
    ],
    'data-sharing': [
      { q: 'How does Secure Data Sharing avoid copying data?', a: 'The provider grants SELECT on views/tables to a Share object and adds consumer accounts; the consumer mounts it as a read-only database. The consumer\'s warehouse reads the provider\'s existing micro-partitions directly — no export, no ETL, no duplicate storage. Changes on the provider side are visible instantly.', difficulty: 'beginner' },
      { q: 'Who pays for compute when a consumer queries shared data?', a: 'The consumer — they run the query on their own virtual warehouse against the provider\'s data. The provider pays only to store the data once. This clean cost split is a big reason sharing scales without the provider funding every consumer\'s queries.', difficulty: 'intermediate' },
      { q: 'How do you share data with a consumer who is NOT on Snowflake?', a: 'Create a managed Reader Account: a limited Snowflake account the provider creates and pays for, into which the share is mounted. The non-Snowflake consumer logs into that reader account to query the data, without needing their own Snowflake contract.', difficulty: 'intermediate' },
      { q: 'What is the difference between a direct share, a listing, and the Marketplace?', a: 'A direct share targets specific known accounts. A listing packages a share with metadata (description, sample SQL, terms) for discovery. The Marketplace is the public catalog of listings (free, paid, or personalized) where you find third-party data or monetize your own.', difficulty: 'intermediate' },
      { q: 'What is a Data Clean Room and when would Netflix use one?', a: 'A clean room lets two parties join sensitive datasets under agreed rules — e.g. aggregate-only, minimum group sizes — without either seeing the other\'s row-level data. Netflix could measure audience overlap with an advertiser: both learn the overlap metrics, neither exposes individual viewers.', difficulty: 'advanced' },
      { q: 'How does cross-region or cross-cloud sharing work under the hood?', a: 'A share is normally same-region. To share across regions/clouds, Snowflake replicates the database to the target region and shares the replica, keeping it refreshed. To the consumer it still looks like a normal share; the replication is what bridges the physical distance.', difficulty: 'advanced' },
    ],
    editions: [
      { q: 'What are the Snowflake editions and how do they differ?', a: 'Standard (core warehouse, 1-day Time Travel, sharing, RBAC), Enterprise (adds up to 90-day Time Travel, multi-cluster warehouses, materialized views, search optimization, column-level security), Business Critical (adds HIPAA/PCI, customer-managed keys, PrivateLink, failover), and Virtual Private Snowflake (a fully isolated deployment). Editions are cumulative.', difficulty: 'beginner' },
      { q: 'Which common features require Enterprise versus Business Critical?', a: 'Enterprise unlocks 90-day Time Travel, multi-cluster (concurrency) warehouses, materialized views, and search optimization. Business Critical adds the security/compliance tier: Tri-Secret Secure customer-managed keys, PrivateLink, database failover, and HIPAA/PCI support. Pick the lowest edition that meets your needs.', difficulty: 'intermediate' },
      { q: 'What is Tri-Secret Secure?', a: 'A Business Critical feature where the data-encryption key is derived from a Snowflake-managed key AND a customer-managed key (in a cloud KMS/HSM). Because both are required, the customer can revoke access unilaterally by disabling their key — strong control for regulated workloads.', difficulty: 'advanced' },
      { q: 'How do you restrict which networks can reach your account?', a: 'Use network policies: allow/deny lists of IP ranges applied at the account or user level. For private connectivity that never touches the public internet, Business Critical adds PrivateLink (AWS/Azure/GCP). Together they lock access to corporate networks.', difficulty: 'intermediate' },
      { q: 'What are the main ways to connect to Snowflake?', a: 'Snowsight (web UI), SnowSQL (CLI), JDBC/ODBC drivers, native connectors (Python, Node.js, Go, Spark, Kafka), and the REST/SQL API for app integrations. All authenticate through the same account and honor its network policies and RBAC.', difficulty: 'beginner' },
    ],
    apps: [
      { q: 'What is Streamlit in Snowflake and why run an app inside Snowflake?', a: 'It lets you build and host interactive Python data apps that execute inside Snowflake on a warehouse, next to the data. Benefits: no separate app server to secure, no data egress, and the same RBAC/masking/row policies apply automatically — so the app can never show a user data their role can\'t see.', difficulty: 'beginner' },
      { q: 'What is the Native Apps Framework?', a: 'It packages data plus logic (stored procedures, UDFs, Streamlit UIs, even models) into an installable application you can distribute and monetize on the Marketplace. Consumers install it into their own account, where it runs on their compute against their data.', difficulty: 'intermediate' },
      { q: 'When would you use Snowpark Container Services?', a: 'When you need to run arbitrary containerized workloads — long-running services, jobs, or GPU workloads — fully managed inside Snowflake\'s security perimeter. It is the escape hatch for logic that doesn\'t fit a SQL/UDF model but should still run next to governed data.', difficulty: 'advanced' },
      { q: 'What is the difference between a UDF and a stored procedure?', a: 'A UDF returns a value and is called inside a SQL statement (per-row or table function); it should be deterministic and side-effect-free. A stored procedure runs procedural logic (loops, DDL/DML, transactions) and is called on its own with CALL. Use UDFs to extend expressions, procedures to orchestrate work.', difficulty: 'intermediate' },
      { q: 'Why write a Python UDF instead of processing data outside Snowflake?', a: 'It runs where the data lives, on warehouse compute, so you avoid exporting data (cost, latency, and a governance/security risk) and you keep RBAC in force. It also lets analysts extend SQL with Python logic without standing up separate infrastructure.', difficulty: 'intermediate' },
    ],
    comparison: [
      { q: 'How does Snowflake differ from Redshift architecturally?', a: 'Redshift historically coupled storage and compute in provisioned clusters (RA3 later separated storage); scaling meant resizing clusters. Snowflake fully separates storage from ephemeral per-second warehouses and adds instant resize plus multi-cluster concurrency. Snowflake is cloud-agnostic; Redshift is AWS-native.', difficulty: 'intermediate' },
      { q: 'How does Snowflake compare to BigQuery?', a: 'Both fully separate storage and compute. BigQuery is serverless with automatic slot allocation (little to tune, GCP-only), while Snowflake gives you explicit warehouses you size and isolate per workload, across AWS/Azure/GCP. Choose BigQuery for zero-ops serverless in GCP, Snowflake for workload isolation and cross-cloud portability plus sharing.', difficulty: 'intermediate' },
      { q: 'When would you pick Databricks over Snowflake?', a: 'When ML and data engineering on a lakehouse lead the work — heavy Spark, notebooks, and open-format (Delta) pipelines on raw files. Snowflake leads for multi-team SQL analytics, governance, and data sharing. Many shops use both; the line is blurring as each adds the other\'s strengths.', difficulty: 'intermediate' },
      { q: 'What is Snowflake\'s standout differentiator versus all three?', a: 'Native, live, zero-copy data sharing plus the Marketplace — sharing governed data across accounts, clouds, and regions with no copying or ETL. Combined with near-zero operational tuning and strong governance, that ecosystem is its clearest edge.', difficulty: 'beginner' },
      { q: 'How do the pricing models differ at a high level?', a: 'Snowflake bills compute per-second by warehouse size plus separate storage. BigQuery bills per-query bytes scanned (on-demand) or reserved slots, plus storage. Redshift bills provisioned node-hours (or serverless RPUs) plus storage. Snowflake\'s per-second + auto-suspend favors bursty workloads; BigQuery on-demand favors sporadic ad-hoc scans.', difficulty: 'advanced' },
    ],
    'e2e-flow': [
      { q: 'Trace a Netflix watch event from click to recommendation.', a: 'The play click emits an event ingested via the Kafka connector / Snowpipe Streaming into a raw VARIANT table; Streams & Tasks (or Dynamic Tables) transform it into fact tables; ML jobs on ML_TRAINING_WH score recommendations into a Dynamic Table; and dashboards/apps read those pre-computed results, accelerated by pruning and caching.', difficulty: 'intermediate' },
      { q: 'Which components ingest, store, transform, and serve in that flow?', a: 'Ingest: Snowpipe / Snowpipe Streaming. Store: columnar micro-partitions in cloud object storage. Transform: Streams + Tasks or Dynamic Tables (plus Snowpark for ML features). Serve: virtual warehouses answering dashboards/apps, sped up by result and SSD caching. Cloud Services orchestrates all of it.', difficulty: 'intermediate' },
      { q: 'Where does Snowpipe Streaming fit versus classic Snowpipe?', a: 'Classic Snowpipe auto-loads files as they land (seconds of latency, file-based). Snowpipe Streaming writes rows directly via channels with sub-second latency and no staged files — the right choice for Netflix\'s ~16k events/second, where waiting to batch into files would add delay and overhead.', difficulty: 'intermediate' },
      { q: 'How do Dynamic Tables reduce dashboard cost in this pipeline?', a: 'Instead of every dashboard re-joining 500B-row fact tables on each load, a Dynamic Table pre-computes the aggregates and incrementally refreshes to a TARGET_LAG (e.g. 15 min). Dashboards then read a small, fresh result — cutting both latency and warehouse credits on the serving side.', difficulty: 'advanced' },
      { q: 'Where do caching and pruning speed up the serving layer?', a: 'Pruning skips the vast majority of micro-partitions using min/max metadata on the query\'s filters, so scans stay tiny. Result cache serves repeated identical dashboard queries instantly, and SSD cache keeps hot partitions on the warehouse. Together they make sub-second executive dashboards over huge tables possible.', difficulty: 'intermediate' },
    ],
    caching: [
      { q: 'Explain the three levels of caching in Snowflake.', a: 'Result cache (Cloud Services) stores a query\'s exact result for 24h and returns it with no warehouse. Local disk (SSD) cache holds micro-partitions on the warehouse nodes for fast re-reads. Metadata cache keeps column statistics permanently, enabling pruning without hitting storage.', difficulty: 'beginner' },
      { q: 'What invalidates the result cache?', a: 'Any DML changing a referenced table, a different query text (even whitespace/LIMIT differences that change results), a different role\'s access, and non-deterministic functions like CURRENT_TIMESTAMP() or RANDOM(). Otherwise the cached result is reused for 24 hours (extended on each hit up to 31 days).', difficulty: 'intermediate' },
      { q: 'How do result cache and local disk cache differ?', a: 'Result cache returns a finished result set with zero compute, account-wide, for identical queries. Local disk cache stores raw micro-partitions on a specific warehouse so it can re-run different queries over the same data faster — it still executes the query, just avoids re-reading storage.', difficulty: 'intermediate' },
      { q: 'Why does suspending a warehouse hurt the next query?', a: 'Suspending evicts the local SSD cache. The next query is a "cold" run that must re-read micro-partitions from remote storage until the cache warms again. That is the small performance cost you trade for the large savings of auto-suspend.', difficulty: 'intermediate' },
      { q: 'Does a result-cache hit consume a warehouse?', a: 'No. The result is served entirely from Cloud Services, so no warehouse needs to be running or resumed. This is why frequently-repeated dashboard queries can cost effectively nothing in compute.', difficulty: 'beginner' },
      { q: 'How would you design queries/workloads to maximize cache hits?', a: 'Standardize query text (BI tools help), avoid non-deterministic functions in cacheable queries, schedule identical refreshes so they hit result cache, keep related workloads on the same warehouse to benefit from SSD cache, and avoid unnecessary DML that invalidates caches.', difficulty: 'advanced' },
    ],
    'data-loading': [
      { q: 'What is the difference between COPY INTO and Snowpipe?', a: 'COPY INTO is a synchronous, transactional batch load you trigger (manually or via a Task) on your warehouse. Snowpipe is serverless and event-driven: cloud storage notifications (or a REST call) auto-run COPY as files arrive, usually within seconds, billed per-credit on Snowflake-managed compute.', difficulty: 'intermediate' },
      { q: 'What is a stage and what types exist?', a: 'A stage is a pointer to files for load/unload. Internal stages are Snowflake-managed (user stage @~, table stage @%tbl, or named internal stage). External stages point to your own S3/Blob/GCS bucket via a storage integration. Iceberg/External tables also read from external locations.', difficulty: 'beginner' },
      { q: 'How does Snowpipe know when to load a file?', a: 'Typically via cloud event notifications (S3 events to SQS/SNS, Azure Event Grid, GCS Pub/Sub) wired to the pipe, which triggers the COPY automatically. Alternatively you call the Snowpipe REST API with the file list. It tracks loaded files to avoid reprocessing.', difficulty: 'intermediate' },
      { q: 'What file sizing is best for bulk loading?', a: 'Aim for compressed files roughly 100–250 MB each and load many in parallel so every warehouse thread stays busy. Too-small files add per-file overhead; single huge files can\'t parallelize. Splitting large inputs into right-sized files is a common load-tuning step.', difficulty: 'advanced' },
      { q: 'How do you handle bad records during a load?', a: 'Use ON_ERROR (CONTINUE, SKIP_FILE, or ABORT_STATEMENT) to control behavior, VALIDATION_MODE to dry-run, and query COPY_HISTORY / rejected-record output to inspect failures. This lets you load the good rows and quarantine the bad ones.', difficulty: 'intermediate' },
      { q: 'How does Snowflake avoid loading the same file twice?', a: 'COPY tracks load metadata (file name + ETag) per table for ~64 days and skips files already loaded unless you set FORCE=TRUE. Snowpipe similarly deduplicates by file, giving idempotent loading without you tracking state.', difficulty: 'advanced' },
    ],
    rbac: [
      { q: 'Explain RBAC: privileges, roles, and users.', a: 'Privileges (SELECT, USAGE, CREATE…) are granted to roles, roles are granted to users and to other roles, and a user activates one role at a time. A user\'s effective access is the union of privileges in their active role\'s inheritance tree. Nothing is granted to users directly.', difficulty: 'beginner' },
      { q: 'What is the difference between SECURITYADMIN and SYSADMIN?', a: 'SYSADMIN creates and owns databases, schemas, warehouses, and other objects. SECURITYADMIN manages identity and grants globally (it contains USERADMIN). Separating them enforces separation of duties: object management vs. access management.', difficulty: 'intermediate' },
      { q: 'What does role inheritance mean?', a: 'Granting role A to role B makes B inherit all of A\'s privileges. This builds a hierarchy — granting custom roles to SYSADMIN means an admin using SYSADMIN can manage everything those roles can. Users get the combined privileges of their active role\'s subtree.', difficulty: 'intermediate' },
      { q: 'Why grant custom roles to SYSADMIN?', a: 'So administrators retain visibility and control over all objects. If a custom role owns objects but isn\'t reachable from SYSADMIN, admins can\'t easily manage them. Rolling custom roles up to SYSADMIN keeps the ownership tree coherent.', difficulty: 'intermediate' },
      { q: 'What is object ownership and why does it matter?', a: 'Every object has exactly one owning role, which has full control and can grant access to others. Ownership matters for who can alter/drop an object and for future grants (GRANT … ON FUTURE TABLES). Poor ownership design leads to objects nobody but ACCOUNTADMIN can manage.', difficulty: 'advanced' },
      { q: 'How would you design roles for a company like Netflix?', a: 'Create functional custom roles (ANALYTICS, ML, MARKETING, FINANCE), grant each only the databases/warehouses it needs, roll them up to SYSADMIN, and reserve ACCOUNTADMIN for rare tasks. Layer masking/row policies for sensitive columns and use SECURITYADMIN/USERADMIN to manage identity separately.', difficulty: 'advanced' },
    ],
    security: [
      { q: 'How does Snowflake encrypt data?', a: 'All data is encrypted at rest with AES-256 (hierarchical, regularly-rekeyed keys) and in transit with TLS 1.2+. Business Critical adds Tri-Secret Secure, combining a Snowflake key with a customer-managed key. Encryption is always on — you can\'t turn it off.', difficulty: 'beginner' },
      { q: 'What is the difference between a masking policy and a row access policy?', a: 'A masking policy transforms a column\'s output based on the querying role (e.g. show *** instead of an email). A row access policy filters which rows a role can see at all. Masking hides values in visible rows; row access hides whole rows.', difficulty: 'intermediate' },
      { q: 'How does dynamic data masking work with roles?', a: 'A masking policy is a function of the value and CURRENT_ROLE()/context, attached to a column. At query time Snowflake evaluates it per role, returning the real value to privileged roles and a masked value to others — without duplicating data or changing the query.', difficulty: 'intermediate' },
      { q: 'How do you audit who accessed sensitive data?', a: 'Query SNOWFLAKE.ACCOUNT_USAGE.ACCESS_HISTORY, which records the columns/objects each query read or wrote. Combined with object tagging, you can prove exactly which roles touched PII — the backbone of compliance reporting.', difficulty: 'intermediate' },
      { q: 'What is the difference between authentication and authorization here?', a: 'Authentication verifies identity (password, SSO/SAML, key-pair, MFA) and is bounded by network policies. Authorization then decides what that identity\'s active role may do via RBAC plus masking/row policies. You need both: who you are, then what you may see.', difficulty: 'beginner' },
      { q: 'How would you enforce that EU subscriber data stays in the EU?', a: 'Run an account (or replicate the relevant databases) in an EU region so the data physically resides there, apply row access policies to keep EU rows scoped to EU-authorized roles, and use masking/tagging for PII. Data residency comes from region placement; policies enforce access on top.', difficulty: 'advanced' },
    ],
    governance: [
      { q: 'What is the difference between RBAC and governance?', a: 'RBAC controls who can connect and which objects they can touch. Governance controls what data they can see within those objects and proves it afterward — tagging, classification, masking/row policies, and access history/lineage. RBAC is the door; governance is what happens inside the room.', difficulty: 'beginner' },
      { q: 'What is tag-based masking and why is it powerful?', a: 'You bind a masking policy to a tag (e.g. PII) once; every column carrying that tag is protected automatically. New columns simply need the tag to inherit protection — so governance scales with your data instead of requiring per-column wiring.', difficulty: 'intermediate' },
      { q: 'What is data classification in Snowflake?', a: 'A built-in capability that scans columns and detects semantic categories (EMAIL, PHONE, NAME) and privacy categories (identifier, quasi-identifier). You can review and then auto-apply system tags, jump-starting a tag-based governance program.', difficulty: 'intermediate' },
      { q: 'How do you build column-level lineage and access auditing?', a: 'ACCOUNT_USAGE.ACCESS_HISTORY records which columns each query read/wrote (including through views), and OBJECT_DEPENDENCIES exposes which objects derive from which. Together they give you audit trails and impact analysis before changes.', difficulty: 'intermediate' },
      { q: 'What is a row access policy versus an aggregation policy?', a: 'A row access policy restricts which rows a role can see. An aggregation policy forces queries to return only aggregated results above a minimum group size, preventing identification of individuals — useful for privacy-preserving analytics and clean rooms.', difficulty: 'advanced' },
      { q: 'How would Netflix meet GDPR/CCPA with Snowflake governance?', a: 'Classify and tag PII, bind masking policies to those tags, apply row access policies for residency/consent scoping, keep EU data in an EU region, and use ACCESS_HISTORY for audit plus Time Travel/streams to honor deletion requests. Governance features make compliance provable, not just claimed.', difficulty: 'advanced' },
    ],
    'cost-performance': [
      { q: 'What are the main drivers of Snowflake cost?', a: 'Compute credits (warehouse size × time × clusters) dominate, then storage (compressed, per-TB), then serverless features (Snowpipe, search optimization, materialized-view/clustering maintenance) and data transfer. Idle-but-not-suspended and oversized warehouses are the usual overspend culprits.', difficulty: 'beginner' },
      { q: 'When do you scale up versus scale out?', a: 'Scale UP (bigger size) to make a single slow, heavy query faster — more memory and CPU per cluster, less spilling. Scale OUT (multi-cluster) to handle more concurrent queries without queuing. Diagnose which problem you have before choosing.', difficulty: 'intermediate' },
      { q: 'What is a resource monitor?', a: 'An object that tracks credit usage against a quota over a period and fires triggers — NOTIFY, SUSPEND, or SUSPEND_IMMEDIATE — at thresholds. Attach it to warehouses (or the account) to cap runaway spend automatically.', difficulty: 'beginner' },
      { q: 'How do you diagnose a slow query?', a: 'Open the Query Profile and find the dominant operator, then check bytes scanned (poor pruning), bytes spilled (warehouse too small), and row explosions across joins (bad join/missing filter). Fix the biggest cost first rather than blindly resizing.', difficulty: 'intermediate' },
      { q: 'When would you add a clustering key?', a: 'On very large tables (roughly multi-TB) where natural ingestion order no longer keeps the columns you filter on well-pruned, and query patterns are consistent. Clustering re-organizes data to restore pruning — but it costs credits to maintain, so only cluster when the query savings justify it.', difficulty: 'advanced' },
      { q: 'Search Optimization vs Materialized View vs Clustering — when each?', a: 'Search Optimization accelerates highly selective point lookups/equality filters on big tables. Materialized Views pre-compute recurring aggregations for fast reads. Clustering improves range pruning on huge tables. They target different query shapes; each adds storage/maintenance cost, so match the tool to the workload.', difficulty: 'advanced' },
    ],
    'data-engineering': [
      { q: 'What are Streams and Tasks?', a: 'A Stream is a change-tracking cursor over a table (the inserts/updates/deletes since last read) — the basis for CDC. A Task runs SQL on a schedule or as a DAG. Combining them lets you incrementally process only new/changed rows.', difficulty: 'beginner' },
      { q: 'What is the difference between standard and append-only streams?', a: 'A standard stream tracks inserts, updates, and deletes (full CDC). An append-only stream tracks only inserts, which is cheaper and ideal for insert-only event pipelines where you never need to react to updates/deletes.', difficulty: 'advanced' },
      { q: 'How does a Dynamic Table differ from a Stream+Task pipeline?', a: 'With Streams+Tasks you imperatively wire change capture and processing. A Dynamic Table is declarative: you write the target query and a TARGET_LAG, and Snowflake manages the incremental refresh. It replaces most hand-built CDC DAGs with far less code.', difficulty: 'intermediate' },
      { q: 'COPY vs Snowpipe vs Snowpipe Streaming?', a: 'COPY is batch, synchronous, warehouse-based. Snowpipe auto-loads files as they arrive (seconds, serverless). Snowpipe Streaming writes rows directly via channels with sub-second latency and no files. Pick by latency need and whether you have files or a row stream.', difficulty: 'intermediate' },
      { q: 'What are Iceberg tables and why use them?', a: 'Apache Iceberg tables store data in open format in your own bucket, readable/writable by Snowflake and other engines. You avoid vendor lock-in and can share one copy across tools, while still getting Snowflake\'s query engine and governance over it.', difficulty: 'intermediate' },
      { q: 'How would you build a near-real-time pipeline for watch events?', a: 'Stream events in with Snowpipe Streaming (Kafka connector) into a raw VARIANT table, use a Dynamic Table (or Stream+Task) to incrementally transform them into fact tables at a short TARGET_LAG, and let dashboards/ML read the pre-computed results — keeping latency low and serving-side cost down.', difficulty: 'advanced' },
    ],
    'business-continuity': [
      { q: 'What is the difference between Time Travel and Fail-Safe?', a: 'Time Travel lets YOU query/restore historical data within the retention window (up to 90 days on Enterprise). Fail-Safe is a further non-queryable 7-day period after that, recoverable only by Snowflake Support — a last resort, not a user feature.', difficulty: 'beginner' },
      { q: 'How far back can Time Travel go?', a: 'Standard edition: 1 day. Enterprise and above: up to 90 days, configurable per object via DATA_RETENTION_TIME_IN_DAYS. Transient/temporary tables cap at 1 day and have no Fail-Safe.', difficulty: 'beginner' },
      { q: 'What protects against a full region outage?', a: 'Only cross-region (and/or cross-cloud) database replication plus failover. Time Travel and Fail-Safe protect against data errors within a region but don\'t help if the whole region is unavailable — you need a replica you can promote.', difficulty: 'intermediate' },
      { q: 'What is the difference between replication and a failover group?', a: 'Replication copies databases to another account/region and keeps them refreshed (read-only replica). A failover group bundles objects (databases, roles, etc.) and lets you promote the secondary to primary during an outage — replication is the copy, failover is the promotion mechanism.', difficulty: 'intermediate' },
      { q: 'What is client redirect?', a: 'A connection URL (secondary connection object) that points clients at whichever account is currently primary. On failover you promote the replica and redirect the connection — applications keep the same URL and don\'t need reconfiguration.', difficulty: 'intermediate' },
      { q: 'Design a DR strategy for Netflix analytics.', a: 'Keep primary in one region with cross-region replication of critical databases to a second region, wrap them in a failover group, and front apps with client redirect. Set Time Travel for human-error recovery, and rehearse failover/failback regularly so RTO/RPO targets are real, not theoretical.', difficulty: 'advanced' },
    ],
    advanced: [
      { q: 'How does Zero-Copy Clone work and what does it cost?', a: 'A clone copies metadata pointers to the source\'s micro-partitions, so it\'s created in milliseconds and stores nothing extra initially. You only pay storage for partitions that change after cloning. It\'s ideal for instant dev/test copies of production.', difficulty: 'intermediate' },
      { q: 'What are common Time Travel use cases?', a: 'Recovering from an accidental DELETE/UPDATE/DROP, comparing a table before/after a load, cloning a table as of a point in time, and auditing historical state. It turns "we broke prod" into a one-line restore within the retention window.', difficulty: 'beginner' },
      { q: 'What is Secure Data Sharing?', a: 'Live, zero-copy sharing of objects with other Snowflake accounts: the consumer queries the provider\'s data in place with their own compute, no copying or ETL, and sees changes instantly. It underpins the Marketplace and cross-org data collaboration.', difficulty: 'beginner' },
      { q: 'What is Snowpark?', a: 'A DataFrame API (Python/Java/Scala) that runs your code inside Snowflake on warehouse compute, next to the data. Netflix uses it for feature engineering and ML pipelines without exporting data to external clusters, keeping governance and avoiding egress.', difficulty: 'intermediate' },
      { q: 'What are Dynamic Tables?', a: 'Declarative, incrementally-refreshed materialized results defined by a query plus a TARGET_LAG freshness goal. Snowflake handles the refresh, replacing much bespoke ETL. Great for pre-computing aggregates that many dashboards read.', difficulty: 'intermediate' },
      { q: 'What problem do Iceberg tables solve?', a: 'They let you keep data in an open, engine-neutral format (Apache Iceberg) in your own storage while querying and writing it with Snowflake — avoiding lock-in and enabling one copy of data shared across Snowflake, Spark, and other engines.', difficulty: 'advanced' },
    ],
    architecture: [
      { q: 'Describe Snowflake\'s three-layer architecture.', a: 'Cloud Services (the brain: optimizer, metadata, auth, RBAC, transactions), Virtual Warehouses (ephemeral compute clusters that run queries), and centralized Storage (columnar micro-partitions in cloud object storage). Each layer scales independently.', difficulty: 'beginner' },
      { q: 'Why separate the three layers?', a: 'So each scales on its own: add compute without moving data, grow storage without paying for compute, and let the always-on services layer serve metadata cheaply. It\'s what enables per-team warehouses, per-second billing, and zero-copy sharing over one copy of data.', difficulty: 'intermediate' },
      { q: 'Is Snowflake shared-disk or shared-nothing?', a: 'A hybrid: like shared-disk, all compute accesses one shared storage tier; like shared-nothing, each warehouse node owns a slice of the work with its own local cache. This combines the simplicity of shared storage with the parallel performance of shared-nothing.', difficulty: 'advanced' },
      { q: 'How does data flow through the layers during a query?', a: 'Cloud Services authenticates, plans, and prunes; if not cached, it dispatches the plan to a virtual warehouse; the warehouse reads needed micro-partitions (from SSD cache or storage), computes, and returns results. Only the warehouse step burns credits.', difficulty: 'intermediate' },
      { q: 'Which layer do you pay for, and how?', a: 'Compute (warehouses) billed per second by size × clusters, and storage billed per compressed TB/month. Cloud Services is generally free unless it exceeds ~10% of daily compute. So most cost tuning targets warehouse usage.', difficulty: 'beginner' },
      { q: 'How does this architecture enable elastic scaling?', a: 'Because compute is stateless and separate from data, you can resize a warehouse instantly, spin up more warehouses for new workloads, or add clusters for concurrency — all against the same storage, with nothing to redistribute or rebalance.', difficulty: 'intermediate' },
    ],
    'cloud-services': [
      { q: 'What is the Cloud Services layer?', a: 'Snowflake\'s always-on, multi-tenant coordination tier: authentication/SSO, the SQL parser and cost-based optimizer, the metadata store, transaction management, and RBAC. It orchestrates every query before any data is scanned.', difficulty: 'beginner' },
      { q: 'What services live in Cloud Services?', a: 'Authentication and access control (RBAC), the query parser and optimizer, the metadata manager (statistics, pruning info, result cache), the transaction manager, and infrastructure management. These are shared across all accounts in the deployment.', difficulty: 'beginner' },
      { q: 'How is the Cloud Services layer billed?', a: 'It\'s free up to roughly 10% of your daily compute credits; usage beyond that is charged. In practice most workloads never pay for it, which is why metadata operations and result-cache hits are effectively free.', difficulty: 'intermediate' },
      { q: 'How does Snowflake\'s optimizer differ from traditional databases?', a: 'It\'s cost-based on automatically-collected micro-partition statistics rather than user-managed indexes and manual stats. There are no indexes to maintain; the optimizer prunes partitions and can serve results from cache — all centrally, independent of the executing warehouse.', difficulty: 'intermediate' },
      { q: 'What happens in Cloud Services when a query is submitted?', a: 'Authenticate, authorize via RBAC, parse/compile, build a cost-based plan and prune micro-partitions, then check the result cache. On a miss it hands the plan to the target warehouse; on a hit it returns immediately. Only execution touches a warehouse.', difficulty: 'intermediate' },
      { q: 'Why can metadata queries run without a warehouse?', a: 'Statistics, object definitions, and result cache live in Cloud Services, so operations like SHOW/DESCRIBE, COUNT via metadata, min/max pruning, and cache hits are answered there directly — no compute cluster needs to resume, saving credits.', difficulty: 'advanced' },
    ],
    compute: [
      { q: 'What is a virtual warehouse?', a: 'An independent cluster of compute nodes that executes queries and loads data. It has its own CPU, memory, and local SSD cache but reads the shared storage tier. You create, size, suspend, and resume warehouses independently per workload.', difficulty: 'beginner' },
      { q: 'What doubles as you go up a warehouse size?', a: 'Each T-shirt size up (XS→S→M→L→XL…) doubles both the compute nodes and the credits/hour. Twice the resources can roughly halve a query\'s runtime, so a size-up is often cost-neutral for a single heavy query while finishing it faster.', difficulty: 'beginner' },
      { q: 'When do you use multi-cluster versus scaling up?', a: 'Scale up (bigger size) for a single slow query needing more memory/CPU. Use multi-cluster (scale out) when many queries run concurrently and queue — extra clusters absorb the concurrency. Different problems, different levers.', difficulty: 'intermediate' },
      { q: 'What are the tradeoffs of auto-suspend and auto-resume?', a: 'Auto-suspend stops billing when idle (big savings) but evicts the SSD cache, so the next query is colder. Auto-resume restarts on demand in ~1–2s. Tune the idle timeout to balance credit savings against cache warmth for your workload.', difficulty: 'intermediate' },
      { q: 'How is workload isolation achieved?', a: 'By giving each workload its own warehouse. Since warehouses share storage but never share compute, one team\'s heavy job can\'t slow another\'s. You isolate simply by provisioning separate warehouses rather than partitioning data.', difficulty: 'intermediate' },
      { q: 'When would you use a Snowpark-optimized warehouse?', a: 'For memory-intensive workloads like ML training and large Snowpark/Python UDFs that need more memory per node than a standard warehouse provides. It trades higher cost per credit for the headroom those workloads require.', difficulty: 'advanced' },
    ],
    storage: [
      { q: 'What is a micro-partition?', a: 'An immutable ~50–500 MB (compressed) columnar file that Snowflake creates automatically as data is ingested, storing per-column metadata (min/max, distinct/null counts). Tables are just large collections of these, and you never manage them manually.', difficulty: 'beginner' },
      { q: 'How does Snowflake prune without user-defined partitions?', a: 'It compares a query\'s WHERE predicates to the min/max (and other) metadata stored per micro-partition and skips any that can\'t contain matching rows — before scanning data. There\'s no PARTITION BY to define; pruning is automatic.', difficulty: 'intermediate' },
      { q: 'Why is columnar storage better for analytics?', a: 'Analytics read a few columns across many rows. Columnar layout reads only those columns (less I/O), compresses better because similar values sit together, and enables vectorized processing. Snowflake uses a hybrid columnar (PAX) format inside each micro-partition.', difficulty: 'beginner' },
      { q: 'How does a micro-partition differ from an HDFS block?', a: 'HDFS blocks are fixed-size, row-oriented byte ranges with no column awareness. Micro-partitions are columnar, self-describing (carry metadata), immutable, and pruning-aware, so Snowflake can skip them by metadata without reading contents — and they\'re created automatically.', difficulty: 'advanced' },
      { q: 'How does clustering help very large tables?', a: 'As huge tables grow, the columns you filter on may spread across many micro-partitions, weakening pruning. A clustering key re-organizes data to co-locate related values, restoring effective pruning — at the cost of automatic clustering credits, so use it only when query savings justify it.', difficulty: 'advanced' },
      { q: 'How is data protected and encrypted at rest?', a: 'Every micro-partition is encrypted with AES-256 under a regularly-rekeyed key hierarchy, and superseded partitions are retained for Time Travel then Fail-Safe. Business Critical adds customer-managed keys via Tri-Secret Secure. Protection and encryption are always on.', difficulty: 'intermediate' },
    ],
  };

  window.SnowflakeViz = window.SnowflakeViz || {};
  window.SnowflakeViz.InterviewBank = InterviewBank;
})();
