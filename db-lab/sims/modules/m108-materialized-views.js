export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">👁️</div>
    <h3>Materialized Views</h3>
    <p>Pre-computed query results — trade write cost for read speed in Prime Day dashboards.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">DDIA — Full animation coming soon</p>
  </div>`;
  return null;
}
