export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🌩️</div>
    <h3>Distributed Failure Lab</h3>
    <p>Inject network partitions, node crashes, message loss — watch the cluster respond.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Labs — Full animation coming soon</p>
  </div>`;
  return null;
}
