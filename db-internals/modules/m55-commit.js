export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">✔️</div>
    <h3>Commit</h3>
    <p>The commit record in the WAL makes the transaction durable — what happens byte-by-byte when Amazon charges your card.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Transactions — Full animation coming soon</p>
  </div>`;
  return null;
}
