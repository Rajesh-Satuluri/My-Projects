export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">📍</div>
    <h3>Checkpoints</h3>
    <p>Fuzzy checkpoints flush dirty pages periodically — bound the recovery time after a Prime Day server crash.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Transactions — Full animation coming soon</p>
  </div>`;
  return null;
}
