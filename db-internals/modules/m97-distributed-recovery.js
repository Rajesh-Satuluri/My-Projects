export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🔄</div>
    <h3>Distributed Recovery</h3>
    <p>Rejoin a failed node, catch up on missed writes — the operational lifecycle of a distributed cluster.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Distributed — Full animation coming soon</p>
  </div>`;
  return null;
}
