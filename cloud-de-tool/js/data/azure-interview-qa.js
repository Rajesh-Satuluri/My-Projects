/* ============================================================
   Cloud DE Visualizer — Azure interview questions, topic-wise.
   Sourced from the uploaded mock-interview transcripts, de-duped
   and given tight, interview-recall model answers. Consumed by
   js/modules/_interview-qa.js.

   Shape: [ { id, label, icon?, blurb?, questions:[ { q, a } ] } ]

   Build progress (10 Q&A / iteration):
     Iter 1  — adf-ir (7) + adf-pipeline (3)            ✅
     Iter 2  — adf-pipeline (+4) + storage (5) + synapse (1)  ✅
     Iter 3  — synapse (+3) + security (4) + monitoring (2) + scenario (1)  ✅
     Iter 4  — scenario (+2) + cicd/design (3)          ✅  [Azure core complete: 35]

   ADF deep-dive expansion (curated ~100 ADF Q&A, tagged by concept):
     ADF-1  — adf-fundamentals (10)                     ✅
     ADF-2  — adf-dataflows (12)                         ✅
     ADF-3  — adf-controlflow (10)                       ✅
     ADF-4  — adf-params (8)                             ✅   [running total: 40]
     ADF-5  — adf-incremental (8)                        ✅
     ADF-6  — adf-security (8)                           ✅
     ADF-7  — adf-cicd (8)                               ✅
     ADF-8  — adf-monitoring (8)                         ✅
     ADF-9  — adf-performance (8)                        ✅
     ADF-10 — adf-functions (8)                          ✅
     ADF-11 — adf-scenarios (12)                         ✅   [ADF deep-dive complete: 100]
   ============================================================ */
(function () {
  'use strict';
  const TV = window.TableViz;

  const TOPICS = [
    {
      id: 'adf-fundamentals',
      label: 'ADF — Fundamentals & vs SSIS/Databricks',
      icon: 'book',
      blurb: 'The opening questions of almost every Azure Data Factory interview: what ADF is, its building blocks, and how it sits next to SSIS, Databricks and Synapse. Get these crisp and you set the tone for the rest of the round.',
      questions: [
        { q: 'What is Azure Data Factory and what problem does it solve?',
          tags: ['Core Concept', 'Architecture', 'ELT'],
          a: 'Azure Data Factory is Azure’s fully managed, serverless data-integration service. It solves the problem of getting data out of many disparate sources — on-prem databases, SaaS APIs, files, cloud stores — into a central place, and orchestrating the steps that clean and reshape it, without you having to run or maintain your own scheduler and servers. You build pipelines of activities that copy, transform and control the flow, then schedule or trigger them and monitor every run from one place. In practice it is the control plane of an Azure data platform: it lands raw data in the lake and orchestrates the Spark or SQL jobs that build the curated layers.' },
        { q: 'What are the core components (building blocks) of ADF?',
          tags: ['Core Concept', 'Architecture'],
          a: 'Five components matter. A pipeline is a logical grouping of activities that together perform a task. An activity is a single step — copy data, run a notebook, run a stored procedure, loop or branch. A dataset is a named view of the data an activity points at — a table or a file path with a format. A linked service is the connection definition — which storage account or database, plus how to authenticate. And the Integration Runtime is the compute that actually executes the activity. Triggers sit on top and decide when a pipeline runs. If you can explain those, you have explained ADF.' },
        { q: 'What is the difference between a linked service and a dataset?',
          tags: ['Linked Service', 'Dataset', 'Core Concept'],
          a: 'A linked service is the connection — it tells ADF which resource to connect to and how to authenticate, for example “this ADLS Gen2 account using a managed identity” or “this Azure SQL database with credentials pulled from Key Vault”. A dataset is the specific object inside that connection — a particular table, or a folder plus file format within the storage account. The relationship is one-to-many: one linked service is reused by many datasets. The simplest way to say it: a linked service is the server, a dataset is the file or table sitting on it.' },
        { q: 'What is the difference between a pipeline and an activity?',
          tags: ['Pipeline', 'Activity', 'Core Concept'],
          a: 'A pipeline is the unit of orchestration and deployment — a DAG of steps that you schedule, parameterize, trigger and monitor as one thing. An activity is a single step inside that pipeline: the smallest unit of work, such as a Copy, a Data Flow, a Lookup or a ForEach. You don’t run an activity on its own; you run the pipeline, and it executes its activities in the order set by their dependencies. So the pipeline is the “what to do and when”, and the activity is each individual thing being done.' },
        { q: 'What are the main types of activities in ADF?',
          tags: ['Activity', 'Control Flow', 'Data Movement'],
          a: 'They fall into three groups. Data movement is essentially the Copy activity, which moves data from a source to a sink. Data transformation activities run compute to reshape data — Mapping Data Flows, a Databricks notebook or jar, a stored procedure, an HDInsight job or an Azure Function. Control flow activities direct the pipeline itself — ForEach, If Condition, Switch, Until, Wait, Lookup, Get Metadata, Set/Append Variable and Execute Pipeline. In an interview it lands better to name one or two from each group than to try to list them all.' },
        { q: 'Is ADF an ETL or an ELT tool?',
          tags: ['ETL', 'ELT', 'Design'],
          a: 'It supports both, but the modern pattern is ELT. In classic ETL you transform data in flight before landing it, which ADF can do with Mapping Data Flows. In ELT — preferred at scale — ADF extracts and loads raw data into the lake or warehouse first, then pushes the transformation down to where the data already lives, typically Databricks or Synapse SQL. ELT wins because it uses the elastic compute of the destination, keeps the raw data available for reprocessing, and avoids moving data twice. So in most designs ADF owns the extract, load and orchestration, and the transform happens downstream.' },
        { q: 'What is the difference between ADF and SSIS?',
          tags: ['Comparison', 'SSIS', 'Migration'],
          a: 'SSIS is the on-prem, server-based ETL tool that ships with SQL Server; ADF is its cloud-native, serverless successor. SSIS runs on a machine you provision and patch, while ADF is fully managed and scales on demand. SSIS transforms data on that box; ADF orchestrates and can push transformation out to Spark or SQL. ADF also brings 100+ cloud connectors, Git and CI/CD integration, and event and tumbling-window triggers out of the box. They are not mutually exclusive — you can lift existing SSIS packages into the cloud by running them on the Azure-SSIS Integration Runtime, which is a very common migration step.' },
        { q: 'How do ADF and Azure Databricks relate — when do you use each?',
          tags: ['Comparison', 'Databricks', 'Orchestration'],
          a: 'They are complementary, not competing. ADF is the orchestrator and connector layer — it schedules, moves data with 100+ connectors, handles retries and monitoring, and triggers other services. Databricks is the transformation engine — a Spark platform for heavy, code-based data engineering and ML. The standard architecture is that ADF lands raw data in the lake and then calls Databricks notebooks or jobs to do the actual Spark transformation, passing parameters in. ADF’s own Mapping Data Flows can transform too, but for complex logic, large volumes, or anything the team wants expressed in code, the transformation goes to Databricks while ADF stays the control plane.' },
        { q: 'What is the difference between ADF and Synapse Pipelines?',
          tags: ['Comparison', 'Synapse', 'Architecture'],
          a: 'Synapse Pipelines are essentially the same pipeline engine as ADF, embedded inside Azure Synapse Analytics — the authoring canvas, activities and Integration Runtimes are nearly identical. The difference is context and scope. Standalone ADF fits when orchestration is your main need and your platform spans many services. Synapse Pipelines fit when you are already all-in on Synapse and want ingestion, Spark, serverless SQL and the warehouse under one workspace and one security model. The caveats: Synapse Pipelines lack a few ADF features such as the SSIS IR and cross-region data flows, and standalone ADF has a broader CI/CD story. Functionally, if you know one you know the other.' },
        { q: 'What is the difference between control flow and data flow in ADF?',
          tags: ['Control Flow', 'Data Flow', 'Core Concept'],
          a: 'Control flow is the orchestration logic of a pipeline — the sequencing, looping and branching between activities: ForEach, If Condition, Until, dependencies, parameters and variables. It decides what runs, in what order and under what conditions, but it does not itself transform row-level data. A data flow — specifically a Mapping Data Flow — is where the actual row-by-row transformation happens: joins, aggregates, derived columns and filters, all compiled to Spark and run on a managed cluster. A clean one-liner for interviews: control flow moves the pipeline along, data flow moves and reshapes the data.' },
      ],
    },
    {
      id: 'adf-ir',
      label: 'ADF — Integration Runtime & Ingestion',
      icon: 'arrow-down',
      blurb: 'The single most-asked Azure Data Factory area: how data physically moves from on-prem into the cloud, and how you make that copy fast and reliable.',
      questions: [
        { q: 'How do you move data from an on-prem source to the cloud using ADF?',
          a: 'Install a Self-hosted Integration Runtime (SHIR) on a machine inside the on-prem network and register it in ADF. Point the source linked service at that SHIR, then a Copy activity pulls the data out through the firewall and lands it in ADLS Gen2 or Blob.' },
        { q: 'Why is the auto-resolve (Azure) Integration Runtime not enough — why do you need a Self-hosted IR?',
          a: 'The Azure IR can only reach public cloud endpoints. On-prem servers sit behind a corporate firewall on a private network, so only a SHIR running inside that network can open the outbound connection and read the source. The Azure IR simply cannot see it.' },
        { q: 'A pipeline copying an on-prem DB to ADLS is running too slowly. How do you troubleshoot it?',
          a: 'Work through the layers: (1) has the data volume grown, (2) is the SHIR node CPU/memory saturated, (3) is the source DB slow (missing indexes, locks), (4) are the copy DIU and parallel-copy / degree-of-parallelism settings too low, (5) is the network the bottleneck. Tune whichever layer is actually pegged.' },
        { q: 'What is DIU and how does it affect copy performance?',
          a: 'Data Integration Units are the compute — CPU, memory and network — allocated to a Copy activity on the Azure IR. Increasing DIU adds parallel throughput and normally shortens the copy, up to the point where the source, sink, or network becomes the new limit. (DIU applies to the Azure IR, not the SHIR.)' },
        { q: 'What does the Get Metadata activity do, and when do you use it?',
          a: 'It returns properties of a dataset — child items, file name, size, last-modified, existence, and column schema — before you process it. Typical uses: enumerate files to drive a ForEach loop, or validate with an If Condition that the expected file actually arrived.' },
        { q: 'What are storage event triggers and when would you use them?',
          a: 'An event trigger starts a pipeline on a Blob/ADLS "blob created" or "blob deleted" event, so ingestion fires the instant a file lands rather than waiting for a schedule. It is the event-driven alternative to a scheduled or tumbling-window trigger for file-arrival pipelines.' },
        { q: 'How do you ensure only one instance of a pipeline runs at any given time?',
          a: 'Set the pipeline concurrency to 1 (or use a tumbling-window trigger with concurrency 1). ADF then queues any subsequent runs until the active run finishes, so you never get overlapping executions.' },
      ],
    },
    {
      id: 'adf-pipeline',
      label: 'ADF — Pipelines, Triggers & Orchestration',
      icon: 'git-branch',
      blurb: 'Building real ADF pipelines: metadata-driven copies, incremental loads, scheduling and failure alerting.',
      questions: [
        { q: 'How do you copy 100 tables from on-prem SQL to ADLS with a single pipeline?',
          a: 'Make it metadata-driven: a Lookup reads the table list (e.g. from information_schema.tables or a control table), a ForEach iterates it, and inside the loop one parameterized Copy activity templatizes the source table and sink path. Everything runs through the SHIR, with watermark logic added if you need incremental.' },
        { q: 'How do you load only incremental data when each table has a different high-watermark column?',
          a: 'Keep a control table of (table_name, watermark_column, last_value). Per table, two Lookups read the column name + last stored value and the current MAX(col); the Copy query filters WHERE col > last_value. After a successful load you update last_value. Parameterizing the column name lets one pipeline serve tables with different watermark columns.' },
        { q: 'How do you send an email when a pipeline fails?',
          a: 'Two common ways: wire the activities’ failure path to a Web or Logic App activity that calls an email/Teams endpoint, or configure an Azure Monitor alert on the pipeline "Failed" run metric that notifies through an Action Group. The Logic App route gives you a richer, custom message.' },
        { q: 'How do you run a pipeline only on the last working day of each month?',
          a: 'A schedule trigger cannot express "last working day" directly. Keep a small calendar file/table listing each month’s last working day, run a plain daily trigger, and add a Lookup + If Condition so the pipeline only proceeds when today matches a date in the calendar.' },
        { q: 'A ForEach copying many tables fails halfway — how do you re-run only the failed tables?',
          a: 'Log each table’s success (to a log file, control table, or ADF variable) as it completes. On rerun, read the log and build the ForEach list from the tables not yet marked successful, so completed tables are skipped and only the failures reprocess.' },
        { q: 'What trigger types does ADF support and when do you use each?',
          a: 'Schedule (wall-clock recurrence), Tumbling Window (fixed non-overlapping slices with dependencies, backfill and concurrency control), Storage Event (blob created/deleted), and Custom Event (via Event Grid). Use tumbling for backfills/dependencies, event for file-arrival, and schedule for simple recurrence.' },
        { q: 'A pipeline fails after loading 500k of 1M rows. Does ADF roll back, and how do you recover?',
          a: 'ADF does not auto-rollback a partial Copy. Make the load idempotent: stage into a temp table/partition and swap on success, or add a pre-step that deletes the target partition (DELETE WHERE load_date = today) before reloading — so a rerun never double-counts.' },
      ],
    },
    {
      id: 'adf-dataflows',
      label: 'ADF — Mapping Data Flows & Transformations',
      icon: 'merge',
      blurb: 'ADF’s code-free transformation engine, compiled to Spark. The deepest technical ADF area interviewers probe — transformations, joins, upserts, debug cost, and when NOT to use it.',
      questions: [
        { q: 'What is a Mapping Data Flow and how does it work under the hood?',
          tags: ['Data Flow', 'Spark', 'Core Concept'],
          a: 'A Mapping Data Flow is ADF’s visual, code-free transformation feature. You draw a graph of transformations — source, join, aggregate, derived column, sink — on a canvas, and ADF compiles that graph into Apache Spark code and runs it on a fully managed, transient Spark cluster it spins up for you. So you get real distributed transformation power without writing or maintaining any Spark code or clusters. It suits teams that need genuine transformation inside ADF but do not have, or do not want, Spark developers. For heavy or highly custom logic, teams still often push the work to Databricks instead.' },
        { q: 'When do you use a Copy activity versus a Mapping Data Flow?',
          tags: ['Data Flow', 'Copy Activity', 'Comparison'],
          a: 'The Copy activity moves data as-is from a source to a sink — it is for ingestion, with only light shaping like column mapping. A Mapping Data Flow transforms data: joins, aggregates, derived columns, pivots and deduplication. The other big difference is cost and startup: Copy runs on the Integration Runtime and is cheap and fast, while a Data Flow warms a Spark cluster (a cold start of a minute or two) and bills by cluster vCore-hours. So the rule is: use Copy when you just need to land data, use a Data Flow when you must reshape it in-flight — and for very heavy transforms, consider pushing to Databricks.' },
        { q: 'What is the difference between Mapping Data Flows and Wrangling Data Flows (Power Query)?',
          tags: ['Data Flow', 'Wrangling', 'Comparison'],
          a: 'Both transform data visually, but they target different users. Mapping Data Flows are the production, engineering-grade option — a designed transformation graph compiled to Spark, meant for scalable, scheduled pipelines. Wrangling Data Flows bring the Power Query experience (the same one in Excel and Power BI) into ADF, letting analysts explore and prep data interactively in a spreadsheet-like grid. Wrangling is great for ad-hoc data preparation and citizen-analyst work; Mapping is what you build robust, large-scale pipelines with. In short: Wrangling for exploratory self-service prep, Mapping for engineered production transformations.' },
        { q: 'What are the main transformations available in a Mapping Data Flow?',
          tags: ['Data Flow', 'Transformations'],
          a: 'They group naturally. Schema and row transformations: Select, Derived Column, Aggregate, Pivot/Unpivot, Window, Rank and Surrogate Key. Multi-input: Join, Lookup, Union and Exists. Row-level: Filter, Sort, Alter Row (tag rows as insert/update/delete/upsert) and Assert (data-quality rules). Hierarchical: Flatten, Parse and Stringify for nested JSON. Plus Source and Sink at the ends. In an interview you do not recite all of them — you name the workhorses (Derived Column, Join, Aggregate, Filter, Alter Row, Flatten) and note that ADF covers the same transformation vocabulary you would expect from Spark or SSIS.' },
        { q: 'How do you use the Derived Column transformation?',
          tags: ['Data Flow', 'Derived Column', 'Transformations'],
          a: 'Derived Column is where you create new columns or overwrite existing ones using ADF’s expression language. It is the main cleansing and enrichment step: trim and change case on strings, cast types, concatenate fields, apply conditional logic with iif() or case(), compute values, parse dates, or replace nulls with coalesce(). You can define many column expressions in one Derived Column transformation, and you can even build column names dynamically for schema-drift scenarios. In practice almost every real data flow has one or more Derived Columns doing the standardisation before data reaches the sink.' },
        { q: 'How does the Flatten transformation work (handling nested JSON)?',
          tags: ['Data Flow', 'Flatten', 'JSON'],
          a: 'Flatten un-nests hierarchical data — typically a JSON file with an array of objects — into a flat, tabular rowset. You point it at the array you want to unroll and it emits one output row per array element, promoting the nested fields into columns; you can also carry parent-level fields down alongside each child row. It is the go-to for turning a JSON payload from an API or event source into rows you can load into a SQL table or Parquet. For deeper structures you chain a Parse to interpret a JSON string first, then Flatten, then a Select to rename the resulting columns.' },
        { q: 'How do you perform an upsert (insert/update) into a SQL sink in a data flow?',
          tags: ['Data Flow', 'Upsert', 'Sink'],
          a: 'Two pieces. First, add an Alter Row transformation and define the policies — for example “Upsert if” a condition, or separate Insert-if and Update-if rules — which tags each row with the intended action. Then, in the Sink, enable the update methods (Allow insert / Allow update / Allow upsert) and specify the key column(s) that identify a matching row. ADF then merges accordingly: matching keys update, new keys insert, as a set-based operation on the SQL side. The point interviewers want is that upsert equals an Alter Row policy plus sink key columns — not row-by-row procedural logic.' },
        { q: 'What is the difference between a broadcast join and a regular join in Mapping Data Flows?',
          tags: ['Data Flow', 'Joins', 'Performance'],
          a: 'A join in Spark normally shuffles both datasets across the cluster so matching keys meet — fine for two large tables. A broadcast join instead sends a full copy of the small side to every node, so the large side joins locally with no shuffle — much faster when one input is small enough to fit in memory. ADF’s Join transformation lets you set broadcast to Auto, Fixed or Off. Use broadcast for a large-fact-to-small-dimension join; turn it off (or leave Auto) if the “small” side is actually large, because broadcasting something too big causes out-of-memory errors. It is the same optimisation you reason about in Spark.' },
        { q: 'What do the Assert and Alter Row transformations do?',
          tags: ['Data Flow', 'Data Quality', 'Alter Row'],
          a: 'Alter Row sets a row-level policy — it tags each row as insert, update, delete or upsert based on conditions, and the sink acts on those tags; it is how you drive updates and deletes into a database sink. Assert is a data-quality gate: you define expectations (expect true, expect unique, expect exists), and rows that fail can be tagged as errors and routed, or the flow can be failed outright. Together they let one data flow enforce quality and apply the right write action: Assert validates, Alter Row decides the DML. Interviewers like hearing Assert used for in-flow data-quality checks rather than a separate validation step.' },
        { q: 'What is Debug mode in Mapping Data Flows and how is it billed?',
          tags: ['Data Flow', 'Debug', 'Cost'],
          a: 'Debug mode spins up an interactive Spark cluster so you can preview data at each transformation as you build — seeing row counts, values and the effect of each step without publishing or running a pipeline. The catch is cost: while the debug session is on, that cluster is running and you are billed for its vCore-hours the whole time it stays alive, not only when you click preview. So the discipline is to turn debug on while actively developing and off when you step away. A Time to Live setting also lets a warm cluster be reused instead of cold-starting each run.' },
        { q: 'What are the data sampling options in Data Flow debug?',
          tags: ['Data Flow', 'Debug', 'Sampling'],
          a: 'When previewing you choose how much data to pull so debugging stays fast and cheap. The options are essentially full dataset (accurate but slow and costly on large data), the first N rows (say the top 1,000 — the usual default for quick iteration), or a random percentage sample of the source. You start with a small first-N sample while wiring up transformations, then optionally validate against fuller data before publishing. The point is to iterate quickly on a subset rather than reprocessing millions of rows just to check an expression.' },
        { q: 'What are the limitations of Mapping Data Flows?',
          tags: ['Data Flow', 'Limitations', 'Design'],
          a: 'The main ones: cold-start latency — each run warms a Spark cluster, adding a minute or two unless you keep a TTL/warm pool — and cost, since you pay for that cluster by vCore-hour. They are less flexible than hand-written Spark for very complex or custom logic, debugging deep graphs can be slow, and some connectors that Copy supports are not available as data-flow sources or sinks. For those reasons many teams cap how much they do in Mapping Data Flows and push heavy or bespoke transformation to Databricks, keeping ADF for orchestration and ingestion. Knowing when not to use them is exactly what senior interviewers probe.' },
      ],
    },
    {
      id: 'adf-controlflow',
      label: 'ADF — Control Flow & Activities',
      icon: 'git-branch',
      blurb: 'The “programming constructs” of ADF: looping, branching, fetching values and calling other pipelines. This is where metadata-driven, dynamic pipelines are actually built.',
      questions: [
        { q: 'What are control flow activities in ADF?',
          tags: ['Control Flow', 'Activity'],
          a: 'Control flow activities direct the pipeline itself rather than move or transform data. They include iteration (ForEach), looping (Until), branching (If Condition, Switch), sequencing and waiting (Wait, activity dependencies), fetching values to drive logic (Lookup, Get Metadata), holding state (Set Variable, Append Variable), and calling other pipelines (Execute Pipeline). Together they let you build dynamic, metadata-driven pipelines — loop over a list of tables, branch on a file’s properties, retry until a condition is met — instead of a fixed linear sequence. They are effectively the programming constructs of ADF.' },
        { q: 'How does the ForEach activity work, and how do you control its parallelism?',
          tags: ['Control Flow', 'ForEach', 'Parallelism'],
          a: 'ForEach iterates over an array — typically the output of a Lookup or Get Metadata — and runs its inner activities once per item, with the current element available via @item(). By default iterations run in parallel, and you control that with the batch count (up to 50); set it to 1 to force sequential execution when order matters or to avoid overwhelming a source. The classic use is metadata-driven ingestion: loop a list of tables and run one parameterized Copy per table. A key gotcha is that a ForEach cannot directly contain another ForEach — you move the inner loop into a child pipeline instead.' },
        { q: 'What is the difference between the ForEach and Until activities?',
          tags: ['Control Flow', 'ForEach', 'Until'],
          a: 'ForEach is a fixed iteration over a known collection — you have the list up front and it processes each element, in parallel or sequentially. Until is a conditional loop — it repeats its activities until an expression becomes true, with no predefined count, like a do-while. Use ForEach when you know the set you are processing (a list of tables or files); use Until when you must poll or wait for a state to change — for example keep checking whether a file has arrived or an external job has finished before continuing. Until also needs a timeout so it cannot loop forever.' },
        { q: 'How does the If Condition activity work?',
          tags: ['Control Flow', 'If Condition'],
          a: 'If Condition evaluates a boolean expression and runs one of two activity branches — the True path or the False path — giving you conditional execution inside a pipeline. Typical uses: check a Get Metadata result and only run the Copy if the file exists or is non-empty; branch on a parameter or on today’s date; proceed only when a row count is above zero. It is single-branch (true/false), so when you have many discrete cases based on one value you reach for Switch instead. Note the branches run activities but the If Condition itself does not return a value you can read afterwards.' },
        { q: 'What is the Switch activity and when do you use it?',
          tags: ['Control Flow', 'Switch'],
          a: 'Switch is multi-way branching: you supply an expression and define several cases, each with its own activities, plus a default case for anything that does not match. It is the cleaner alternative to nesting several If Conditions when routing on one value — for example running different logic per file type (CSV vs JSON vs Parquet), per region, or per source-system code. Each case is mutually exclusive and only the matching one executes. Reach for Switch whenever you catch yourself chaining If Conditions on the same variable.' },
        { q: 'What do the Lookup and Get Metadata activities do, and how do they differ?',
          tags: ['Control Flow', 'Lookup', 'Get Metadata'],
          a: 'Both fetch information to drive later steps, but from different places. Lookup reads data content — it runs a query or reads a file and returns rows (a single row or a limited row set), so you use it to pull a control-table list, a config value or a stored watermark. Get Metadata returns properties about a dataset rather than its contents — existence, size, last-modified, item count, child items, column schema. So: Lookup for “what’s in the data” (give me the table list, the last load date), Get Metadata for “facts about the file or folder” (does it exist, how big, what’s inside). They are frequently used together just ahead of a ForEach.' },
        { q: 'What is the difference between the Set Variable and Append Variable activities?',
          tags: ['Control Flow', 'Variables'],
          a: 'Both write to pipeline variables, but Set Variable assigns a single value, replacing whatever was there — used for scalars like a flag, a counter or a computed string. Append Variable adds an element to an array variable, keeping the existing contents — used to accumulate a list across a loop, for example collecting the names of processed files or failed tables. A common gotcha: Append Variable inside a parallel ForEach is not safe, because concurrent iterations clash, so you either set the ForEach to sequential or collect results another way. Set for scalars, Append for building arrays.' },
        { q: 'How do you build parent-child pipelines with the Execute Pipeline activity?',
          tags: ['Control Flow', 'Execute Pipeline', 'Orchestration'],
          a: 'The Execute Pipeline activity lets one pipeline call another, so you build a master (parent) pipeline that orchestrates several child pipelines, each owning one logical stage such as ingest, transform or load. You pass parameters down to the child, and the “Wait on completion” setting controls whether the parent waits for it to finish before continuing. Benefits are modular, reusable pipelines, cleaner monitoring, and a way to sequence dependencies across teams. It is also how you get around the no-nested-ForEach limit — put the inner loop in a child pipeline the parent calls inside its ForEach.' },
        { q: 'Explain the Lookup + ForEach + Copy pattern for dynamic pipelines.',
          tags: ['Control Flow', 'Metadata-Driven', 'Pattern'],
          a: 'This is the canonical metadata-driven ingestion pattern. A Lookup activity reads a control table (or config file) listing what to process — table names, source paths, watermark columns. A ForEach iterates that returned list, and inside it a single parameterized Copy activity uses @item() to fill in the source object and sink path for the current entry. So one small pipeline ingests hundreds of tables generically; to onboard a new table you add a row to the control table, not a new pipeline. Add a watermark Lookup inside the loop and it becomes incremental. This pattern comes up in almost every ADF interview.' },
        { q: 'Can ADF orchestrate non-data tasks, and how do you add a delay between activities?',
          tags: ['Control Flow', 'Wait', 'Orchestration'],
          a: 'Yes — ADF is a general orchestrator, not only a data mover. It can call Azure Functions, Web/REST endpoints, Logic Apps, stored procedures or delete files, so it can trigger and coordinate non-data tasks as part of a workflow. To pause between steps you use the Wait activity, which simply delays the pipeline for a specified number of seconds — useful for spacing out API calls, giving a downstream system time to settle, or building a simple poll-and-wait loop together with Until. So orchestration of arbitrary steps plus Wait for timing are both first-class.' },
      ],
    },
    {
      id: 'adf-params',
      label: 'ADF — Parameters, Variables & Expressions',
      icon: 'sliders',
      blurb: 'What makes ADF pipelines reusable instead of hard-coded: parameters vs variables, global parameters, the expression language, and multi-environment parameterization.',
      questions: [
        { q: 'What is the difference between parameters and variables in ADF?',
          tags: ['Parameters', 'Variables', 'Core Concept'],
          a: 'Parameters are inputs passed into a pipeline (or dataset, linked service or data flow) at the start of a run and are read-only during execution — you use them to make a pipeline reusable, for example a table name or a date the caller supplies. Variables are internal, mutable values that live for the duration of the run and can be changed with Set/Append Variable — you use them to hold state, counters, or to accumulate a list as the pipeline executes. In short: parameters come in from outside and don’t change; variables are scratch storage you update as you go.' },
        { q: 'What are global parameters and when do you use them?',
          tags: ['Parameters', 'Global Parameters'],
          a: 'Global parameters are constants defined at the factory level and available to every pipeline, rather than declared per pipeline. They are ideal for environment-wide values — an environment name, a base storage path, an API root — that many pipelines reference. Because they can be overridden per environment during CI/CD (through the ARM template parameters), they are a clean way to inject Dev/QA/Prod differences without editing each pipeline. So: global parameters for shared, environment-specific constants; regular parameters for per-run inputs.' },
        { q: 'How do you pass parameters to a pipeline at runtime?',
          tags: ['Parameters', 'Triggers'],
          a: 'You declare parameters on the pipeline, then supply values when it starts — from a trigger definition (each trigger can pass its own values, including system variables like the trigger’s scheduled time), from the Execute Pipeline activity when a parent calls a child, from the Trigger-now/Debug dialog manually, or via the REST API, PowerShell or SDK when an external system kicks it off. Inside the pipeline you read them with @pipeline().parameters.name. This is what makes one pipeline reusable across many runs and callers.' },
        { q: 'How do you pass parameters between pipelines (parent to child)?',
          tags: ['Parameters', 'Execute Pipeline'],
          a: 'The parent uses an Execute Pipeline activity and, in its settings, maps values into the child pipeline’s declared parameters — those values can be literals, the parent’s own parameters, variables, or expressions such as @item() inside a ForEach. The child reads them with @pipeline().parameters.name. ADF has no true return value from a child, so to pass data back you either write it to a store the parent reads, or design the child to be fire-and-forget. This parameter hand-off is what makes the master/child orchestration pattern reusable.' },
        { q: 'How do you pass arrays or complex objects as parameters?',
          tags: ['Parameters', 'Arrays', 'Expressions'],
          a: 'Parameters and variables support Array and Object types, so you can pass a JSON array or a structured object, not just strings. A common pattern is a Lookup returning an array you feed straight into a ForEach, or passing an array of config objects into a child pipeline. You build and read them with expression functions — createArray(), json(), and indexing or dot access like @variables(\'cfg\')[0].tableName. Watch the typing: values often arrive as strings and need json() to be treated as an object. Arrays and objects are what let metadata-driven pipelines carry rich per-item config.' },
        { q: 'How do you manage parameterization across multiple environments (Dev/QA/Prod)?',
          tags: ['Parameters', 'Multi-Environment', 'CI/CD'],
          a: 'You never hard-code environment-specific values. Endpoints, storage paths and Key Vault URIs live in linked services parameterized by global parameters (or linked-service parameters), and during CI/CD the ARM template exposes those as template parameters that the release pipeline overrides per stage. Secrets themselves stay in Key Vault, referenced indirectly, so only the vault URI changes between environments. The result is that the exact same published artifact promotes from Dev to QA to Prod with only the parameter values swapped — no manual edits in higher environments, which is precisely what interviewers want to hear.' },
        { q: 'What are expressions and dynamic content in ADF, and how are they used?',
          tags: ['Expressions', 'Dynamic Content'],
          a: 'ADF has an expression language — the “dynamic content” behind most fields — that computes values at runtime instead of hard-coding them. It exposes functions and system variables: @pipeline().parameters, @variables(), @activity(\'X\').output to read a prior activity’s result, @utcnow(), plus string, date, collection and logical functions like concat(), formatDateTime(), coalesce() and iif(). You use it everywhere — dynamic sink file names, filtered Copy queries, ForEach items, If Condition tests. Mastering @activity().output chaining and the common functions is what makes pipelines genuinely dynamic and metadata-driven.' },
        { q: 'What is a wildcard path in a source dataset, and when would you use it?',
          tags: ['Datasets', 'Wildcard', 'Ingestion'],
          a: 'A wildcard path lets one source dataset match many files by pattern instead of a single fixed name — using * and ? in the folder or file name, for example bronze/orders/2024-*/*.csv. The Copy activity then reads every matching file in one pass, which is perfect when files land with dated names or you want all files in a partitioned folder. It pairs well with parameterized paths so the pattern itself is dynamic per run. Use it whenever the file name isn’t known exactly or you’re ingesting a whole set of files rather than one.' },
      ],
    },
    {
      id: 'adf-incremental',
      label: 'ADF — Incremental & Metadata-Driven',
      icon: 'refresh',
      blurb: 'How you move only new or changed data and drive hundreds of tables from one pipeline — watermarking, control tables, CDC, dedup and idempotency. Core to any real production platform.',
      questions: [
        { q: 'How do you implement incremental loading in ADF?',
          tags: ['Incremental', 'Watermark', 'Design'],
          a: 'Incremental loading means moving only data that is new or changed since the last run, instead of reloading everything — essential for cost and runtime at scale. The standard approach is watermarking: you track a high-water value per table (a last-modified timestamp or an ever-increasing id) in a control table, and each run copies only rows greater than the stored value, then updates it. Alternatives are source-side Change Data Capture for row-level deltas, or file-based deltas (only new files by name or last-modified). For files landing in the lake, an event trigger plus a last-modified filter achieves the same idea. The recurring interview point: never full-load when you can watermark.' },
        { q: 'How does watermark-based incremental loading work in detail?',
          tags: ['Incremental', 'Watermark', 'Control Table'],
          a: 'You keep a control table with columns like (table_name, watermark_column, last_value). At the start of a run one Lookup reads the stored last_value and another reads the current MAX(watermark_column) from the source. The Copy activity’s source query then filters WHERE watermark_column > last_value AND <= current_max, capturing exactly the new slice. After the Copy succeeds, a Stored Procedure updates last_value to current_max. Parameterizing the column name lets one pipeline serve tables with different watermark columns. Bounding the upper end (<= current_max) avoids missing rows that arrive mid-run — a detail that impresses interviewers.' },
        { q: 'How do you build a metadata-driven ingestion framework in ADF?',
          tags: ['Metadata-Driven', 'Control Table', 'Pattern'],
          a: 'Instead of one pipeline per table, you build a single generic pipeline driven by a control (metadata) table that lists every source: table name, source path, sink path, load type, watermark column and last value. A Lookup reads that table, a ForEach iterates the rows, and inside the loop one parameterized Copy (plus optional watermark Lookups) handles the current entry via @item(). Onboarding a new source becomes inserting a row, not authoring a pipeline. It scales to hundreds of tables, centralises configuration, and is the framework pattern almost every serious ADF role expects you to describe.' },
        { q: 'How do you copy only new or changed files from a source folder?',
          tags: ['Incremental', 'Files', 'Ingestion'],
          a: 'It depends on how files arrive. If names carry a date or sequence, use a parameterized wildcard path so each run reads only that day’s folder. If not, use the Copy activity’s built-in incremental file filter on last-modified datetime — set the start/end window so only files modified since the last run are copied. Or use an event trigger so a pipeline fires the instant a blob is created, processing exactly that file. For robustness many teams also record processed file names in a control table and skip anything already logged, which handles late-arriving or reprocessed files too.' },
        { q: 'How do you deduplicate records in an ADF pipeline?',
          tags: ['Deduplication', 'Data Flow', 'Data Quality'],
          a: 'The cleanest way is a Mapping Data Flow: use a Window transformation with row_number() partitioned by the business key and ordered by a recency column, then a Filter to keep rank = 1 — that keeps the latest version of each duplicate. For a plain distinct you can Aggregate grouping by all columns. On the sink side, an upsert (Alter Row plus key columns) stops duplicates accumulating across runs, and at the file level a control table skips already-processed files. Interviewers like the row_number()-then-filter pattern because it correctly handles “keep the newest duplicate”.' },
        { q: 'How do you make a pipeline idempotent so reruns don’t double-load?',
          tags: ['Idempotency', 'Reliability', 'Design'],
          a: 'Idempotent means running the pipeline twice produces the same result with no duplicates, so a retry after a partial failure is always safe. ADF does not roll back a partial Copy, so you engineer it: stage into a temp table or partition and swap on success; or delete the target slice before reloading it (DELETE WHERE load_date = @date, then insert); or upsert on a key so re-inserted rows overwrite rather than duplicate. Combined with watermark bounds so you always reprocess a well-defined slice, this makes reruns deterministic. It is one of the most-valued design principles in a DE interview.' },
        { q: 'How do you handle Change Data Capture (CDC) with ADF?',
          tags: ['CDC', 'Incremental', 'Sources'],
          a: 'ADF supports change data capture at a few levels. Some sources (SQL Server, Azure SQL) expose native CDC or change tracking, and the Copy or Data Flow source reads only the changed rows. ADF also has a dedicated top-level CDC resource and a change-data-capture option in Mapping Data Flow sources that tracks inserts, updates and deletes natively for supported stores. Where the source has no CDC, you fall back to watermarking on a modified-date or id column. The design goal is the same — capture deltas, not full snapshots — so you pick native CDC when available and watermarking when not.' },
        { q: 'How do you capture and use a “last processed” state across runs?',
          tags: ['State Management', 'Control Table', 'Incremental'],
          a: 'You persist state outside the pipeline, because a run has no memory of the previous one. The usual store is a control table (in Azure SQL or similar) holding the last watermark or last processed file per source; a Lookup reads it at the start and a Stored Procedure updates it at the end, inside the success path so a failure doesn’t advance the pointer. Lightweight alternatives are a small state file in the lake, or pipeline variables within a single run. The key discipline is updating state only after a successful load, so failures re-process rather than skip data.' },
      ],
    },
    {
      id: 'adf-security',
      label: 'ADF — Security & Key Vault',
      icon: 'shield',
      blurb: 'How you keep credentials, identity and network access secure — Key Vault, managed identities, RBAC, private networking and ADLS permissions. A favourite senior-level probing area.',
      questions: [
        { q: 'What are the main security features of ADF?',
          tags: ['Security', 'Overview'],
          a: 'ADF’s security rests on several layers. Identity and access use Microsoft Entra ID plus Azure RBAC, so who can view or edit the factory is role-controlled. Secrets never live in pipeline JSON — they sit in Azure Key Vault and are referenced indirectly, or avoided entirely via managed identities. Data is encrypted in transit and at rest by default, with an option for customer-managed keys. Network exposure is controlled with a Managed Virtual Network, private endpoints and IP firewall rules. And everything is auditable through Azure Monitor logs. In an interview, frame it as identity, secrets, encryption, network and audit.' },
        { q: 'How do you integrate Azure Key Vault with ADF?',
          tags: ['Key Vault', 'Secrets'],
          a: 'You create a Key Vault linked service in ADF, then grant ADF’s managed identity access to the vault — typically the Key Vault Secrets User role or an access policy with Get/List on secrets. Other linked services then reference their passwords, keys or connection strings as Key Vault secret references rather than storing the value inline. At runtime ADF fetches the secret from the vault, so the credential never appears in the pipeline definition or in source control. This is the standard, expected way to handle any secret in ADF, and it also centralises rotation.' },
        { q: 'What is a managed identity and how does ADF use it?',
          tags: ['Managed Identity', 'Authentication'],
          a: 'A managed identity is an Entra identity that Azure creates and manages for the Data Factory itself — system-assigned (tied to the factory’s lifecycle) or user-assigned (a standalone identity you can share). ADF authenticates as that identity to other services, so you grant it RBAC roles — for example Storage Blob Data Contributor on ADLS — and it accesses them with no stored credential at all. That eliminates keys and connection strings from pipelines, and there is nothing to rotate or leak. It is the preferred auth method for ADF reaching Azure resources; Key Vault covers the cases where a real secret is unavoidable.' },
        { q: 'How do you control who can access and edit ADF (RBAC)?',
          tags: ['RBAC', 'Access Control'],
          a: 'Access to author and operate ADF is controlled by Azure RBAC at the factory or resource-group scope. Built-in roles matter: Data Factory Contributor lets someone create and edit pipelines, Reader gives view-only, and custom roles can separate who may trigger runs from who may change definitions. You assign roles to Entra users and groups — best practice is groups, not individuals. Combined with Git integration, so changes flow through pull requests, this separates “who can develop” from “who can deploy to production”. Interviewers want to hear least-privilege and role assignment via groups.' },
        { q: 'How do you secure credentials for on-prem or third-party source systems?',
          tags: ['Security', 'Credentials', 'SHIR'],
          a: 'For sources reached through a Self-hosted IR, credentials are encrypted and stored against the SHIR, and the actual secret values are pulled from Key Vault rather than typed into the linked service. The SHIR communicates outbound over HTTPS, so you don’t open inbound firewall ports. Where the source supports it, prefer token- or certificate-based auth over passwords, and use a least-privilege service account. The principles are the same as everywhere in ADF: no plaintext secrets in definitions, Key Vault as the source of truth, least privilege, and encryption in transit.' },
        { q: 'How do you restrict ADF network access (Managed VNet, private endpoints, IP rules)?',
          tags: ['Networking', 'Private Endpoint', 'Managed VNet'],
          a: 'Three complementary controls. A Managed Virtual Network for the Azure IR keeps data-movement compute inside a managed VNet so traffic doesn’t traverse the public internet. Private endpoints give linked services — storage, SQL, Key Vault — private IPs reachable only from that VNet. And IP firewall rules on the target resources restrict which addresses may connect. Together they let you lock ADF down so it reaches sources over private networking with no public exposure — a common requirement in regulated environments and a strong senior-level answer.' },
        { q: 'How do you handle secret rotation for linked services secured via Key Vault?',
          tags: ['Key Vault', 'Secret Rotation'],
          a: 'Because linked services reference Key Vault secrets indirectly rather than storing the value, rotation is handled in the vault, not in ADF. You add the new secret version in Key Vault and ADF picks it up on the next run — no pipeline redeploy needed. If you pin a specific secret version you update the reference; referencing the latest version avoids even that. Better still, managed identities remove the secret entirely, so there is nothing to rotate. The interview point: centralising secrets in Key Vault is what makes rotation a non-event.' },
        { q: 'What are the two levels of security in ADLS Gen2 that ADF relies on?',
          tags: ['ADLS', 'RBAC', 'ACL'],
          a: 'ADLS Gen2 security combines two mechanisms. First, Azure RBAC roles at the storage-account or container level (for example Storage Blob Data Reader or Contributor) give coarse-grained access. Second, POSIX-style ACLs on individual directories and files give fine-grained, path-level control — read, write and execute for specific identities. ADF’s managed identity is granted the appropriate RBAC role and, where needed, directory ACLs so it can read the bronze path but not others. Knowing that RBAC is coarse and ACLs are fine-grained, and that they work together, is exactly what the question is testing.' },
      ],
    },
    {
      id: 'adf-cicd',
      label: 'ADF — CI/CD & Dev→Prod',
      icon: 'git-branch',
      blurb: 'Getting pipelines to production safely: Git integration, ARM templates, the adf_publish branch, branching strategy and automated multi-environment deployment. The lead-level questions.',
      questions: [
        { q: 'How does Git integration work in ADF?',
          tags: ['CI/CD', 'Git', 'Source Control'],
          a: 'You connect the Data Factory to an Azure DevOps or GitHub repository, so all pipeline, dataset and linked-service definitions are stored as JSON in source control instead of only in the live service. Developers work on feature branches, commit and raise pull requests into a collaboration branch (usually main), and get versioning, code review and rollback. Crucially only the Dev factory is Git-connected; higher environments are deployed to, never edited directly. Git integration is the foundation everything else in ADF CI/CD builds on.' },
        { q: 'What is the adf_publish branch and what are ARM templates in ADF?',
          tags: ['CI/CD', 'ARM Template', 'Deployment'],
          a: 'When you click Publish in the Dev factory, ADF generates ARM (Azure Resource Manager) templates — a JSON description of every pipeline, dataset, linked service and trigger — and commits them to a special adf_publish branch. Those templates, with a companion parameters file, are the deployable artifact: a release pipeline takes them and deploys to QA and Prod. Environment-specific values are exposed as template parameters and overridden per stage. So ARM templates are how ADF turns your factory into infrastructure-as-code that promotes cleanly across environments.' },
        { q: 'Walk through promoting an ADF pipeline from Dev to Prod.',
          tags: ['CI/CD', 'Deployment', 'Multi-Environment'],
          a: 'Development happens only in the Git-connected Dev factory. When ready you Publish, which generates ARM templates on the adf_publish branch. A release pipeline (Azure DevOps or GitHub Actions) then deploys those templates to QA, and after validation and an approval gate, to Prod — overriding environment-specific parameters (endpoints, Key Vault URIs, storage paths) at each stage. Triggers are usually stopped before deployment and restarted after. Nobody edits QA or Prod by hand. The headline for interviewers: the same artifact promotes across environments with only parameters changing, gated by approvals.' },
        { q: 'What branching strategy do you use for ADF development?',
          tags: ['CI/CD', 'Git', 'Branching'],
          a: 'A typical model is feature branches off a main (collaboration) branch: each developer creates a short-lived feature branch in the Git-connected factory, does the work, then raises a pull request into main with review. Only main is published, which generates the adf_publish artifacts. Some teams add release branches for hardening. The important behaviours are no direct commits to main, PR review before merge, and publish/deploy only from the collaboration branch — so production always traces back to reviewed, merged code.' },
        { q: 'How do you automate ADF deployment with Azure DevOps or GitHub Actions?',
          tags: ['CI/CD', 'Automation', 'DevOps'],
          a: 'The CD pipeline picks up the ARM templates from the adf_publish branch and runs an ARM deployment task against each target factory, passing a per-environment parameters file. Best practice adds pre- and post-deployment scripts to stop triggers before deploying and restart them after, and to clean up resources removed from the templates. Approval gates sit between stages. The point is that deployment is a repeatable, reviewed pipeline — not someone clicking Publish in Prod.' },
        { q: 'How do you handle environment-specific values during deployment?',
          tags: ['CI/CD', 'Parameters', 'Multi-Environment'],
          a: 'Anything that differs between environments — storage accounts, database endpoints, Key Vault URIs, folder paths — is parameterized, never hard-coded. Global parameters and linked-service parameters surface as ARM template parameters, and the release pipeline supplies a different parameters file per stage. Secrets themselves stay in Key Vault and are referenced, so only the vault URI changes. You can also customise which properties become parameters via the arm-template-parameters-definition file. The result: one template, many environments, differing only by parameter values.' },
        { q: 'What is the difference between Git mode and Live mode in ADF Studio?',
          tags: ['CI/CD', 'Git', 'Publishing'],
          a: 'Live mode edits the published factory directly — changes take effect immediately and there is no version history, which is fine for a quick look but risky and not how you should work. Git mode connects the factory to a repo, so your edits are commits on a branch, you get history and pull requests, and nothing is live until you Publish. Best practice is to always work in Git mode in Dev and treat Live mode as emergency-only. Interviewers ask this to check you understand that production should not be hand-edited.' },
        { q: 'How do you build reusable pipeline templates for common patterns?',
          tags: ['CI/CD', 'Templates', 'Reusability'],
          a: 'Two levels. ADF has pipeline templates — the template gallery plus custom templates saved to the repo — that let you stamp out a common pattern like SFTP-to-Blob or copy-all-tables as a starting point. More powerfully, you build parameterized, metadata-driven pipelines so one pipeline handles many sources by configuration rather than copies, and share parameterized linked services and datasets to avoid duplication. The design goal is DRY: capture a pattern once and reuse it, so onboarding a new source is config, not new code.' },
      ],
    },
    {
      id: 'adf-monitoring',
      label: 'ADF — Monitoring, Logging & Cost',
      icon: 'activity',
      blurb: 'Operating pipelines in production: the Monitor tab, rerun-from-failure, Azure Monitor / Log Analytics, custom audit logging, alerting and cost control. The operational-maturity questions.',
      questions: [
        { q: 'How do you monitor pipelines in ADF?',
          tags: ['Monitoring', 'Operations'],
          a: 'The built-in Monitor tab in ADF Studio is the first stop: it lists every pipeline run with status, duration, trigger, parameters and run ID, and lets you drill into activity-level detail and rerun. For platform-wide observability you route ADF diagnostic logs to Azure Monitor and Log Analytics and query them with KQL, build dashboards, and set alert rules. For proactive notification you configure alerts on failed runs through Action Groups. So: the Monitor tab for per-run inspection, Azure Monitor plus Log Analytics for aggregate monitoring and alerting.' },
        { q: 'How do you rerun a failed pipeline, and how does rerun-from-failure work?',
          tags: ['Monitoring', 'Rerun', 'Recovery'],
          a: 'From the Monitor tab you can rerun a pipeline run — either the whole pipeline or, importantly, from the point of failure, which re-executes only the failed activity and everything downstream while skipping activities that already succeeded. That saves time and avoids reprocessing completed work. You can also rerun with different parameters. For this to be safe the reprocessed activities should be idempotent (stage-and-swap or delete-before-load) so a partial rerun doesn’t double-count. Rerun-from-failure plus idempotent design is the recovery story interviewers look for.' },
        { q: 'How do you set up alerts and failure notifications for pipelines?',
          tags: ['Monitoring', 'Alerts', 'Azure Monitor'],
          a: 'You set up alerts through Azure Monitor. Create an alert rule on an ADF metric — most commonly “Failed pipeline runs” — or on a Log Analytics query, and attach an Action Group that notifies via email, SMS, Teams or a webhook. For richer, custom messages you instead wire the pipeline’s failure path to a Web or Logic App activity that sends a formatted email including the pipeline name and error. Many teams use both: Monitor alerts as a safety net and a Logic App for detailed, actionable notifications.' },
        { q: 'How do you send ADF logs to Log Analytics, and why?',
          tags: ['Monitoring', 'Log Analytics', 'Diagnostics'],
          a: 'You enable Diagnostic Settings on the Data Factory and route categories like PipelineRuns, ActivityRuns and TriggerRuns to a Log Analytics workspace. Once there the logs are queryable with KQL, so you can build dashboards, track trends such as failure rates and durations, correlate across services, and set log-based alerts. It also gives retention beyond the Monitor tab’s limited window. The reason it matters: the Monitor tab is per-run and short-lived, while Log Analytics gives durable, aggregate, queryable observability across the whole platform.' },
        { q: 'How do you implement custom logging and auditing for every run?',
          tags: ['Monitoring', 'Audit', 'Logging'],
          a: 'Beyond the built-in logs you add your own audit trail: at key points a Stored Procedure or Copy writes a row to a log table capturing pipeline name, run ID via @pipeline().RunId, start and end times, row counts and status. Copy activity output exposes rowsRead and rowsCopied you can persist. Wrapping this in a reusable log-start / log-end / log-error pattern across pipelines gives a consistent, queryable audit history for reconciliation and compliance. Interviewers like seeing @pipeline().RunId used to correlate custom logs with ADF’s own run history.' },
        { q: 'How do you monitor and control the cost of ADF?',
          tags: ['Cost', 'Monitoring', 'Optimization'],
          a: 'ADF cost comes from activity runs, Data Integration Units times duration for Copy, Data Flow cluster vCore-hours, and SHIR/orchestration charges. You monitor it with Azure Cost Management filtered or tagged to the factory, and control it by design: minimise Data Flow cold starts with a TTL, right-size DIU rather than maxing it, prefer incremental over full loads, turn off Data Flow debug sessions when idle, and consolidate tiny activities. Tagging resources lets you attribute spend. The theme: most ADF cost is Data Flows and data volume, so incremental loading and cluster discipline are the big levers.' },
        { q: 'What are the common reasons a Copy activity fails, and how do you troubleshoot?',
          tags: ['Troubleshooting', 'Copy Activity', 'Reliability'],
          a: 'Common causes: authentication or permission errors (expired secret, missing RBAC or ACL), source/sink schema or type mismatches, network or firewall issues (especially through a SHIR), throttling from the source or sink, timeouts on large transfers, and malformed source data. You troubleshoot from the activity’s error message and output in the Monitor tab, check SHIR health and logs for on-prem sources, use fault-tolerance settings to skip and log incompatible rows, and add retries for transient errors. A structured diagnosis — auth, schema, network, throttling, data — is what you want to convey.' },
        { q: 'How do you audit pipeline executions for compliance and traceability?',
          tags: ['Audit', 'Compliance', 'Governance'],
          a: 'Compliance auditing combines ADF’s own run logs routed to Log Analytics (a durable, queryable history of what ran, when and with what outcome) with your custom audit table capturing row counts and status per run, plus lineage from Microsoft Purview or captured in your logging. RBAC and Git history record who changed pipeline definitions. Together these answer the auditor’s questions: what data moved, when, by which process, was it complete, and who changed the code. Framing it as run history plus data lineage plus change history covers the compliance angle well.' },
      ],
    },
    {
      id: 'adf-performance',
      label: 'ADF — Performance & Optimization',
      icon: 'zap',
      blurb: 'Making pipelines fast and cost-efficient: DIUs and parallel copy, partitioning, data skew, Data Flow cluster tuning, large-file handling and finding the real bottleneck.',
      questions: [
        { q: 'How do you optimize the performance of an ADF Copy activity?',
          tags: ['Performance', 'Copy Activity', 'DIU'],
          a: 'Work through the layers. Raise Data Integration Units so the Azure IR has more parallel throughput, and enable parallel copy so large sources move in concurrent partitions. Use a source partition option (physical partition, key range, or dynamic range) so partitions transfer together. Choose an efficient format and staged copy — via Blob with PolyBase or COPY INTO for Synapse sinks. Make sure the bottleneck isn’t the source DB (indexes, locks) or the SHIR node’s CPU and network. In short: partition the source, raise DIU and parallelism, stage for warehouses, and remove the real bottleneck.' },
        { q: 'What are DIUs and parallel copy, and how do they affect throughput?',
          tags: ['Performance', 'DIU', 'Parallelism'],
          a: 'Data Integration Units are the bundle of CPU, memory and network the Azure IR allocates to a Copy activity; more DIUs generally means more throughput, up to where the source, sink or network becomes the limit. Parallel copy is a separate dial — it splits the data into partitions moved concurrently within the activity. They work together: DIUs provide the horsepower, parallel copy provides the concurrency to use it. Note DIUs apply to the Azure IR, not the SHIR (which you scale by adding nodes). Over-provisioning DIUs beyond the bottleneck just adds cost with no speedup.' },
        { q: 'How does partitioning improve performance in ADF?',
          tags: ['Performance', 'Partitioning', 'Parallelism'],
          a: 'Partitioning lets ADF process chunks of data in parallel instead of one serial stream. In Copy, source partitioning (physical partitions or dynamic key ranges) splits a large table so partitions transfer concurrently. In Mapping Data Flows, the partitioning scheme (round-robin, hash, range) controls how Spark distributes rows across the cluster, which drives join and aggregate performance. Writing to partitioned sink folders, for example by date, also speeds later reads. The general principle: partition on a good key to get parallelism and to avoid one node doing all the work.' },
        { q: 'How do you handle data skew in Mapping Data Flows?',
          tags: ['Performance', 'Data Skew', 'Data Flow'],
          a: 'Data skew is when one partition holds far more rows than others — often a hot key — so one Spark task runs long while the rest finish, dragging out the whole data flow. You address it by choosing a better partitioning strategy or key in the data flow’s Optimize tab (hash on a high-cardinality column, or repartition), by filtering or aggregating skewed values earlier, and by broadcasting the small side of a join so the skewed large side isn’t shuffled. Recognising skew from an uneven task duration and fixing the partition key is the mature answer.' },
        { q: 'How do you optimize Mapping Data Flow cluster performance and cost?',
          tags: ['Performance', 'Data Flow', 'Cost'],
          a: 'Because Data Flows run on Spark clusters billed by vCore-hours, performance and cost move together. Keep a compute Time to Live so successive activities reuse a warm cluster instead of cold-starting each time; right-size the cluster core count to the data; tune the Optimize tab partitioning to avoid skew; push filters and column pruning early so less data moves; and avoid unnecessary Data Flows where a Copy or a push-down to Databricks or SQL would be cheaper. The execution plan shows where time is spent. The theme: warm clusters, right sizing and early filtering.' },
        { q: 'How do you ingest and process very large files efficiently or in chunks?',
          tags: ['Performance', 'Large Files', 'Parallelism'],
          a: 'For a very large source you avoid one giant serial transfer. In Copy, enable parallel copy with a source partition (range or physical) so the table or file moves in concurrent chunks. For many files, a ForEach over ranges or wildcard-scoped copies parallelises the work. On the compute side, Mapping Data Flows and Databricks naturally partition large inputs across the cluster, and you write output to partitioned folders. The goal is to break the work into parallel units sized to the IR or cluster rather than pushing everything through a single thread.' },
        { q: 'How do you identify and remove pipeline bottlenecks?',
          tags: ['Performance', 'Troubleshooting', 'Monitoring'],
          a: 'Start from the Monitor tab’s activity durations and the Copy activity’s detailed performance breakdown, which shows time in queue, at source, in transfer and at sink, plus throughput and DIUs used — that tells you which stage is slow. For Data Flows the execution plan and stage timings reveal skew or an expensive transformation. Then attack the actual bottleneck: source DB tuning, more DIU or parallelism, cluster sizing, or network. The disciplined message is measure first with the built-in breakdown, then tune the stage that’s actually pegged — don’t guess.' },
        { q: 'How does Data Flow auto-scaling and integration runtime sizing work?',
          tags: ['Performance', 'Auto-Scaling', 'Integration Runtime'],
          a: 'For Data Flows you can set the integration runtime’s compute to auto-scale within a core range, so ADF allocates more cores for heavier data and fewer for light loads, balancing speed and cost. For the Copy/Azure IR you scale by raising DIUs (auto or fixed) and parallel copy. The Self-hosted IR scales differently — you add nodes to a cluster for high availability and throughput. Choosing an appropriately sized IR and letting Data Flow compute auto-scale, rather than pinning one large fixed cluster, is the cost-aware answer.' },
      ],
    },
    {
      id: 'adf-functions',
      label: 'ADF — Functions & Expressions',
      icon: 'file-text',
      blurb: 'The expression language that makes pipelines dynamic: system variables, reading activity output, and the string, date, null and collection functions you reach for every day. A practical cheat-topic.',
      questions: [
        { q: 'What system variables does ADF expose in expressions?',
          tags: ['Functions', 'System Variables', 'Expressions'],
          a: 'ADF exposes system variables you reference in expressions to get run context. The common ones: @pipeline().RunId (the unique run identifier, great for logging), @pipeline().Pipeline (the pipeline name), @pipeline().TriggerTime and TriggerType, and inside a ForEach @item() for the current element. You also read @pipeline().parameters.name and @variables(\'name\'). The trigger’s scheduled time (@trigger().scheduledTime, or the tumbling-window start and end) drives time-sliced loads. Naming RunId for logging and the trigger time for incremental slicing shows you actually use them.' },
        { q: 'How do you read another activity’s output in an expression?',
          tags: ['Functions', 'Activity Output', 'Expressions'],
          a: 'You reference a previous activity’s result with @activity(\'ActivityName\').output, then drill into the returned JSON. For a Lookup returning one row it is @activity(\'Lookup1\').output.firstRow.columnName; for a row set, @activity(\'Lookup1\').output.value is the array you feed to a ForEach. For Get Metadata it is @activity(\'GetMeta\').output.exists or .childItems. Copy exposes @activity(\'Copy1\').output.rowsCopied. This output-chaining is the backbone of dynamic pipelines — one activity’s result drives the next — so knowing the firstRow versus value distinction on Lookup is a frequent interview check.' },
        { q: 'What are the most useful string functions in ADF?',
          tags: ['Functions', 'String', 'Expressions'],
          a: 'The workhorses are concat() to build strings such as paths and queries, substring() and replace() to edit them, toLower() / toUpper() and trim() to standardise, split() to tokenise, and indexOf() / contains() for tests. length(), startsWith() and endsWith() help with validation and file-name logic. You use these constantly for dynamic file names and paths, for cleansing values in Derived Columns, and for building filter predicates. Being able to rattle off concat, replace, substring, split and toLower shows fluency with the expression language.' },
        { q: 'What date/time functions do you use most, and how do you format dates?',
          tags: ['Functions', 'Date', 'Expressions'],
          a: 'The key ones: utcnow() for the current timestamp, addDays() / addHours() / addMinutes() for windows (for example addhours(utcnow(), -24) for “last 24 hours”), and formatDateTime() to render a date in a specific pattern — essential for dynamic dated folder paths like formatDateTime(utcnow(), \'yyyy/MM/dd\'). startOfDay() and startOfMonth() help build slices, and ticks() lets you compare timestamps numerically. Date handling drives incremental windows and partitioned paths, so formatDateTime for paths and addDays/addHours for windows are the ones to know.' },
        { q: 'How do you handle nulls and conditional logic in expressions?',
          tags: ['Functions', 'Null Handling', 'Logic'],
          a: 'For nulls, coalesce() returns the first non-null of its arguments — the standard way to default a missing value — and you can test with empty() or isNull(). For branching logic, iif(condition, trueValue, falseValue) is the inline if, and there is a switch/case-style expression for multi-way choices. In Mapping Data Flows the equivalents are iif(), isNull() and coalesce() inside Derived Columns. Using coalesce() to handle nulls and iif() for conditional values is exactly the graceful null-handling interviewers ask about.' },
        { q: 'What collection and array functions does ADF provide?',
          tags: ['Functions', 'Collections', 'Arrays'],
          a: 'For arrays and objects: createArray() builds an array, length() counts it, first() / last() and indexing with [0] access elements, contains() tests membership, and union() / intersection() combine sets. json() parses a string into an object you can navigate, and you pass arrays straight into a ForEach. These are what make metadata-driven pipelines work — a Lookup returns an array, you iterate it, and you read per-item fields. Knowing json() to turn a string into a navigable object is a common sticking point worth mentioning.' },
        { q: 'How do you build a dynamic file path or query with expressions?',
          tags: ['Functions', 'Dynamic Content', 'Ingestion'],
          a: 'You combine functions and system variables in dynamic content. A dated sink path might be @concat(\'bronze/orders/\', formatDateTime(utcnow(), \'yyyy/MM/dd\'), \'/\'). A filtered source query is built with concat() using @item().table for the table name and @variables(\'lastWatermark\') for the cutoff. A unique file name might fold in @pipeline().RunId. The pattern is concat() to assemble the string plus formatDateTime(), parameters and @item() for the variable parts. Showing a concrete dynamic path or query with concat and formatDateTime lands this well.' },
        { q: 'What is the difference between @ and @{} in ADF expressions?',
          tags: ['Functions', 'Syntax', 'Expressions'],
          a: 'A leading @ starts an expression that returns a value — you use it when the whole field is the expression, for example a dynamic filter query or a boolean for an If Condition. @{} is string interpolation — it embeds an expression’s result inside surrounding literal text, for example file_@{formatDateTime(utcnow(), \'yyyyMMdd\')}.csv. So use bare @ when the field is entirely the expression, and @{} when you are splicing a computed value into a larger string. Wrapping a whole-value expression in @{} unnecessarily is a classic small mistake, so the distinction is worth stating.' },
      ],
    },
    {
      id: 'adf-scenarios',
      label: 'ADF — Scenario-Based',
      icon: 'sparkles',
      blurb: 'The situational “how would you design this” prompts — fault tolerance, on-prem migration, REST ingestion, near-real-time, master/child orchestration and event-driven files. Where you prove you can wire it all together.',
      questions: [
        { q: 'Your pipeline fails intermittently due to network issues — how do you make it fault-tolerant?',
          tags: ['Scenario', 'Fault Tolerance', 'Retry'],
          a: 'Make the pipeline resilient rather than assuming a perfect network. Set retry count and interval on the Copy and other activities so transient failures self-heal. Enable Copy fault tolerance to skip and log incompatible rows instead of failing the whole run. Design the load to be idempotent (stage-and-swap or delete-before-load) so a retry after a partial failure doesn’t duplicate data. Add rerun-from-failure for manual recovery and an alert on failure. For on-prem sources, run the SHIR with multiple nodes for high availability. Retries plus idempotency plus fault tolerance plus an HA SHIR is the recipe.' },
        { q: 'How would you copy data from on-prem SQL Server to Azure SQL Database?',
          tags: ['Scenario', 'On-Prem', 'Migration'],
          a: 'Install and register a Self-hosted IR on a machine inside the on-prem network. Create a linked service for the source SQL Server through the SHIR and one for the Azure SQL target, with credentials from Key Vault. Define source and sink datasets, then a Copy activity moves the data — in bulk for the first load, and thereafter incrementally via a watermark so only new rows move. Add retries and monitoring, then schedule with a trigger. The key components to name are the SHIR for connectivity, Key Vault for secrets, and watermarking for ongoing loads.' },
        { q: 'How would you transform a CSV in Blob Storage and load it, code-free?',
          tags: ['Scenario', 'Data Flow', 'Transformation'],
          a: 'Use a Mapping Data Flow so no Spark code is needed. The source reads the CSV from Blob; then you chain transformations — Derived Column to cleanse and cast, Filter to drop bad rows, Aggregate or Join to reshape, maybe Alter Row for upserts — and a Sink writes the result to the target (Azure SQL, Parquet in the lake, and so on). A pipeline wraps the data flow and a trigger runs it. This is the canonical “transform without writing code” answer, contrasted with Copy (which only moves data) and Databricks (for heavier logic).' },
        { q: 'How would you copy many CSV files from ADLS with a single pipeline?',
          tags: ['Scenario', 'ForEach', 'Metadata-Driven'],
          a: 'Make it metadata-driven. Use Get Metadata to list the files in the ADLS folder, a ForEach to iterate them, and one parameterized Copy inside the loop that uses @item().name for the current file and a templated sink path. Set the ForEach batch count for controlled parallelism. If the files share a schema you can even point a single Copy at a wildcard path and move them all at once. One parameterized pipeline handling N files, rather than N pipelines, is the point.' },
        { q: 'How would you ensure one pipeline runs only after another succeeds?',
          tags: ['Scenario', 'Dependencies', 'Orchestration'],
          a: 'Two clean options. Use an Execute Pipeline activity in a master pipeline that calls A then B with “wait on completion”, so B starts only after A finishes successfully (a success dependency). Or, for decoupled pipelines, use tumbling-window triggers with a trigger dependency so B’s window depends on A’s completion. Within a single pipeline, activity dependencies (the green success arrows) enforce ordering. Some teams also have A write a done-marker that B checks. Success dependencies via Execute Pipeline, or tumbling-window dependencies, is what they want.' },
        { q: 'How would you implement incremental loading to move only new/changed data from on-prem?',
          tags: ['Scenario', 'Incremental', 'Watermark'],
          a: 'Track a watermark per table in a control table. Through the SHIR, a Lookup reads the last stored value and another reads the current MAX of the watermark column; the Copy query filters rows greater than the last value up to the current max; on success a Stored Procedure updates the watermark. A ForEach makes it multi-table and metadata-driven. For sources with native CDC you read change tables instead. This moves only new or changed rows each run — the standard on-prem incremental pattern combining SHIR, watermark and control table.' },
        { q: 'How would you integrate multiple formats (CSV, JSON, Parquet) into one dataset?',
          tags: ['Scenario', 'Multi-Format', 'Union'],
          a: 'Read each format with its own source in a Mapping Data Flow — CSV, JSON (often with a Flatten for nested arrays) and Parquet — standardise each to a common schema with Select and Derived Column, then combine them with a Union transformation into one dataset before the sink. If schemas drift, enable schema drift and use rule-based mapping. A pipeline orchestrates it, possibly using Get Metadata plus Switch to route by file type first. The core idea is normalise-then-Union inside a data flow to integrate heterogeneous formats into one output.' },
        { q: 'How would you copy from a REST API into Azure SQL, handling pagination?',
          tags: ['Scenario', 'REST API', 'Pagination'],
          a: 'Use a REST linked service and the Copy activity’s built-in pagination rules, which follow next-page links or increment an offset/page parameter until the API returns no more data — no manual looping for the common cases. Store the key or token in Key Vault. When pagination is non-standard, fall back to an Until loop with a Web activity that calls each page and appends results, tracking the cursor in a variable. Land the JSON in the lake, then Flatten and load into Azure SQL. Naming the Copy activity’s native pagination support is the crisp answer.' },
        { q: 'How would you design near-real-time processing with ADF and Event Hub?',
          tags: ['Scenario', 'Streaming', 'Near-Real-Time'],
          a: 'ADF is batch and micro-batch, not a true stream processor, so for near-real-time you combine services. Event Hub ingests the stream; Azure Stream Analytics or Databricks Structured Streaming processes it in near real time and lands results in ADLS or Synapse; ADF then orchestrates the downstream movement and transformation on a frequent schedule or an event trigger. If seconds-level latency is required, the streaming engine does the real-time part and ADF handles the orchestration around it. The honest, senior answer names ADF’s batch nature and puts a streaming service in front for the real-time leg.' },
        { q: 'How would you design a master pipeline orchestrating multiple child pipelines?',
          tags: ['Scenario', 'Orchestration', 'Execute Pipeline'],
          a: 'A master pipeline uses Execute Pipeline activities to call each child in the right order, passing parameters and waiting on completion so dependencies hold — for example ingest, then transform, then load, each a separate child. This modularises the solution: children are independently testable and reusable, monitoring is cleaner, and you sidestep the no-nested-ForEach limit by putting loops in children. You add failure handling per branch and can run independent children in parallel. Modular children coordinated by a parent via Execute Pipeline is exactly the orchestration pattern asked for.' },
        { q: 'How would you trigger a pipeline whenever a file is uploaded to a container?',
          tags: ['Scenario', 'Event Trigger', 'Ingestion'],
          a: 'Create a storage event trigger bound to the Blob/ADLS container that fires on a blob-created event, optionally filtered by path prefix and file suffix, for example only the /incoming folder and .csv files. The trigger passes the file name and path to the pipeline via system variables, so it processes exactly the file that landed — no polling. Ensure ADF’s managed identity has the needed storage role and that Event Grid is enabled. An event trigger with a path/suffix filter passing the file path into the pipeline is the complete answer.' },
        { q: 'How would you process only the files uploaded in the last 24 hours?',
          tags: ['Scenario', 'Files', 'Filtering'],
          a: 'Use the Copy activity’s last-modified filter: set modifiedDatetimeStart to @addhours(utcnow(), -24) and leave the end open (or set utcnow()), so only files modified in that window are copied. Alternatively, Get Metadata returns each file’s lastModified and a Filter or If Condition checks it against the 24-hour cutoff before processing in a ForEach. For dated folder layouts you can instead build the path with expressions. The neat answer is the built-in last-modified window using addhours(utcnow(), -24) rather than manually looping and comparing.' },
      ],
    },
    {
      id: 'storage',
      label: 'ADLS & Storage',
      icon: 'folder',
      blurb: 'The storage layer under every Azure lakehouse: Blob vs ADLS Gen2, the hierarchical namespace, and how compute reaches and secures the data.',
      questions: [
        { q: 'What is the difference between Blob Storage and ADLS Gen2?',
          a: 'ADLS Gen2 is Blob Storage with the Hierarchical Namespace enabled. That adds true directories with atomic folder rename/move plus POSIX ACLs, making it analytics-optimized. Plain Blob is a flat object store, better for unstructured/backup data. Same underlying platform — Gen2 layers analytics features on top.' },
        { q: 'What does the Hierarchical Namespace add, and why does it matter for analytics?',
          a: 'It gives real directories instead of flat key prefixes, so folder operations (rename/move/delete) are atomic and cheap, and you get directory-level POSIX ACLs. That makes partitioned folder layouts and fine-grained path security work efficiently — essential for a Spark/lakehouse workload.' },
        { q: 'What is the difference between ADLS Gen1 and Gen2?',
          a: 'Gen1 was a standalone HDFS-like service; Gen2 is built on Blob Storage with the hierarchical namespace, so you inherit Blob’s tiering, redundancy and cost model plus the analytics features. Gen2 is the current standard and Gen1 is retired — always design on Gen2.' },
        { q: 'How does Databricks access ADLS Gen2 — mount vs direct access?',
          a: 'Either mount the container once to a DBFS path (dbutils.fs.mount) so notebooks use a friendly /mnt path, or access directly via the abfss:// URI with credentials in Spark config. Modern practice under Unity Catalog is direct access through external locations / storage credentials rather than mounts.' },
        { q: 'How do you give a service passwordless, least-privilege access to ADLS?',
          a: 'Use a managed identity (or service principal) and grant it an Azure RBAC role such as Storage Blob Data Reader/Contributor, optionally narrowed with POSIX ACLs on specific folders. No account keys or SAS tokens in pipelines — any secret that is genuinely needed lives in Key Vault.' },
      ],
    },
    {
      id: 'synapse',
      label: 'Synapse & Serving Layer',
      icon: 'cpu',
      blurb: 'The warehouse / serving side: what Synapse is, its pool types, and how BI reaches the curated data.',
      questions: [
        { q: 'What does Azure Synapse Analytics do, and where does it fit in a DE architecture?',
          a: 'Synapse is an integrated analytics platform that bundles several engines behind one workspace: a dedicated SQL pool (an MPP data warehouse), serverless SQL (pay-per-query over files already in the lake), Apache Spark pools, and Synapse Pipelines (essentially ADF). In a lakehouse it usually plays the serving/warehouse role — curated Gold data is either loaded into a dedicated pool for fast, concurrent BI, or left in the lake and queried with serverless SQL — and Power BI sits on top. The value is that ingestion, big-data processing, warehousing and serving live in one governed workspace instead of four separate services.' },
        { q: 'What is the difference between a Synapse dedicated SQL pool and a serverless SQL pool?',
          a: 'A dedicated SQL pool is a provisioned MPP warehouse: you pre-buy compute in DWUs, data is physically stored and distributed across 60 nodes, and it delivers fast, highly concurrent queries — but you pay for it whether or not it is running, so it suits stable, heavy BI workloads. Serverless SQL has no infrastructure to manage: it queries Parquet/CSV/Delta files in ADLS on demand and bills per terabyte scanned, which is ideal for ad-hoc exploration, lightweight transforms, or querying the lake without loading it first. Rule of thumb: dedicated for predictable production warehouses, serverless for exploratory or intermittent access where you do not want idle cost.' },
        { q: 'In a Synapse dedicated pool, how do you avoid expensive data movement on a large fact-to-dimension join?',
          a: 'Data movement happens when matching rows for a join live on different distributions and Synapse has to shuffle them across nodes. The fix is choosing distributions deliberately: HASH-distribute the large fact table on the join key so rows that join land on the same distribution, and REPLICATE small dimension tables so a full copy sits on every node and the join stays local. Reserve ROUND_ROBIN for staging/loading where there is no obvious key. Combined with up-to-date statistics and columnstore indexes, co-locating the join key is the single biggest MPP tuning lever.' },
        { q: 'When would you choose Databricks over Synapse (and vice versa)?',
          a: 'They overlap but lean different ways. Databricks is a Spark-first lakehouse — code-centric (PySpark/SQL/Scala), Delta-native, strong for large-scale ETL, streaming, ML and data-science collaboration. Synapse is warehouse-first and T-SQL–centric, with the tightest integration into the Azure/Power BI/BI-analyst world and a familiar SQL surface. Many real architectures use both: Databricks does the heavy medallion ETL and writes Delta/Gold, and Synapse (or Databricks SQL) serves it to Power BI. Pick by team skills and workload — Spark/ML/streaming leans Databricks, SQL-warehouse-and-BI leans Synapse.' },
      ],
    },
    {
      id: 'security',
      label: 'Security & Governance',
      icon: 'shield',
      blurb: 'Keeping the platform secure and compliant: secrets, identity vs permissions, cataloguing, and PII handling — a favourite area for senior-level probing.',
      questions: [
        { q: 'How do you pass credentials securely in ADF and Databricks?',
          a: 'Never hard-code secrets in pipeline JSON, notebooks, or configs. Store them in Azure Key Vault and reference them indirectly: in ADF via a Key Vault linked service so linked-service passwords resolve at runtime, and in Databricks via a Key Vault-backed secret scope read with dbutils.secrets.get(). Better still, avoid stored secrets entirely where you can by authenticating services with a managed identity, so there is no password to leak or rotate. Access to the vault itself is then controlled by RBAC and audited.' },
        { q: 'How does authentication differ from authorization on Azure, and which services provide each?',
          a: 'Authentication answers "who are you" and is handled by Microsoft Entra ID (formerly Azure AD), which issues the identity/token for users, groups, service principals and managed identities. Authorization answers "what are you allowed to do" and is handled by Azure RBAC — role assignments scoped to a subscription, resource group, or resource. On ADLS the two combine: Entra proves identity, RBAC grants coarse storage roles, and POSIX ACLs on directories/files add fine-grained path-level control on top. Keeping the two concepts separate is exactly what interviewers listen for.' },
        { q: 'What is Microsoft Purview and how does it relate to Unity Catalog?',
          a: 'Purview is an estate-wide data governance service: it scans sources across Azure, on-prem and other clouds to build a data map — catalogue, business glossary, automated classification of sensitive data, and end-to-end lineage. Unity Catalog governs inside the Databricks lakehouse specifically, enforcing table/column/row permissions, masking and lineage at query time. They are complementary layers rather than competitors: Purview gives the organisation-wide "what data exists and where did it come from" view, while Unity Catalog is the enforcement point that actually allows or blocks a query in Databricks. Rated PARTIAL in the cross-cloud matrix for that reason.' },
        { q: 'How do you handle PII, data masking and GDPR compliance in a pipeline?',
          a: 'Start by classifying and tagging sensitive columns (Purview or Unity Catalog tags) so you know where PII lives. Apply the least-privilege principle: encrypt at rest and in transit (default on ADLS), restrict access with RBAC/ACLs, and expose sensitive fields through dynamic data masking or column-level masks and row-level security so most consumers never see raw values. For GDPR specifics, keep lineage to prove where personal data flows, support deletion/"right to be forgotten" — which Delta makes feasible via DELETE + VACUUM — and pseudonymise or hash identifiers in lower environments. The theme is: classify, minimise exposure, encrypt, mask, and be able to audit and delete.' },
      ],
    },
    {
      id: 'monitoring',
      label: 'Monitoring & Ops',
      icon: 'sliders',
      blurb: 'How you observe pipelines in production and debug failures — the operational maturity questions.',
      questions: [
        { q: 'Which Azure services do you use to monitor pipelines and inspect logs?',
          a: 'Azure Monitor is the umbrella platform for metrics, alerts and dashboards; Log Analytics is the query store underneath it, where you route diagnostic logs and run KQL queries. For data workloads you send ADF and Databricks diagnostic settings into a Log Analytics workspace, then build alert rules (e.g. on pipeline "Failed" runs or job duration) that notify through Action Groups — email, Teams, SMS, or a webhook. ADF also has its own Monitor tab for per-run/per-activity status, and Databricks surfaces the Spark UI and job run history for job-level detail.' },
        { q: 'A production job fails intermittently — what is your approach to monitor and debug it?',
          a: 'First make failures observable: ensure diagnostic logs flow to Log Analytics and that an alert fires on failure so you are not finding out from users. When one fires, reproduce the timeline from the run history — which activity/stage failed, with what error — and pull the detailed logs (ADF activity output, or the Spark UI / driver+executor logs in Databricks) to find the root cause. Classify it: data issue (bad/late/skewed input), resource issue (OOM, under-sized cluster), or transient (throttling, a flaky dependency). Then fix at the right layer — add data-quality checks and retries for data/transient problems, right-size or tune compute for resource problems — rather than just re-running blind.' },
      ],
    },
    {
      id: 'scenario',
      label: 'Scenario & System Design',
      icon: 'git-branch',
      blurb: 'End-to-end pipeline design prompts — the whiteboard questions where you show you can wire the Azure services together.',
      questions: [
        { q: 'Design a real-time pipeline: on-prem SQL Server → ADLS → Databricks → Power BI, with email alerts on failure.',
          a: 'Capture changes at the source with CDC (SQL Server change tracking / CDC, or a tool like Debezium) rather than re-reading whole tables, and land the change events into ADLS Gen2 as a Bronze layer — either through Event Hubs for a truly streaming feed or an ADF/SHIR pull for micro-batch. Databricks then processes it with the medallion pattern: Structured Streaming or Auto Loader ingests Bronze, cleans and de-dupes into Silver Delta tables, and aggregates business metrics into Gold. Power BI connects to a Databricks SQL warehouse (or Synapse) over the Gold tables, using DirectQuery or short refreshes so the dashboard reflects new data quickly. For reliability, orchestrate with Databricks Workflows or ADF, add data-quality expectations (DLT) between layers, and wire failure paths to an alert — an Azure Monitor rule or Logic App — that emails the on-call. Call out the trade-off explicitly: true streaming (Event Hubs + Structured Streaming) for seconds-level latency, micro-batch (ADF + scheduled jobs) when a few minutes is acceptable and simpler to run.' },
        { q: 'Design a pipeline that pulls from an API every 5 minutes into ADLS, then builds Bronze/Silver/Gold Delta tables, with email on failure.',
          a: 'A scheduled trigger (ADF or Databricks Workflows) fires every 5 minutes and calls the API — a Copy activity or a small notebook — landing the raw JSON into ADLS Gen2 as the Bronze layer, partitioned by ingestion time. A Databricks job (ideally Auto Loader or DLT) then picks up only the new Bronze files, parses/cleans/de-dupes them into Silver Delta tables, and a second step aggregates business metrics into Gold fact tables. Chain the steps as a Workflow so Silver waits on Bronze and Gold on Silver, add DLT expectations for data quality between layers, and rely on checkpoints so each run processes only new data. Wire the job’s failure path to an Azure Monitor alert or Logic App that emails the on-call. Note the trade-off: a 5-minute micro-batch is simpler and cheaper than true streaming and is the right choice unless you genuinely need sub-minute latency.' },
        { q: 'How would you migrate a 1 TB on-prem database (or Spark workload) to Azure?',
          a: 'Split it into a one-time historical load plus ongoing incremental. For the 1 TB history, use ADF with a Self-hosted IR to bulk-copy into ADLS Gen2 — maximising DIU and parallel copy, staging as Parquet/Delta; if the network is the bottleneck, use partitioned parallel copies or physically ship data with Azure Data Box. Then stand up incremental/CDC so daily deltas flow via watermark queries or change tracking rather than reloading everything. If it is a Spark workload moving to Databricks, lift the code onto Databricks clusters, repoint storage to ADLS (abfss / Unity Catalog external locations), convert tables to Delta, and re-tune cluster sizing and partitioning for the new environment. Finally validate with row counts/checksums between source and target, run old and new in parallel for a cutover window, then switch consumers over.' },
      ],
    },
    {
      id: 'cicd',
      label: 'CI/CD & Design Principles',
      icon: 'git-branch',
      blurb: 'Getting code to production safely, and the principles behind a well-designed pipeline — the senior/lead-level questions.',
      questions: [
        { q: 'How do you promote ADF pipelines from Dev to QA/Prod (CI/CD)?',
          a: 'Connect the Dev factory to a Git repo (Azure DevOps or GitHub) so work happens on feature and collaboration branches, never directly in the live service. Publishing generates ARM templates on the adf_publish branch, and a release pipeline deploys those templates to QA and Prod. Environment-specific values — linked-service endpoints, Key Vault URIs, storage accounts — are parameterised and supplied per stage, so the same template promotes cleanly, with approval gates between environments. The point interviewers listen for: no manual editing in higher environments; everything flows through source control and automated deployment.' },
        { q: 'How do you do CI/CD for Databricks notebooks and jobs?',
          a: 'Keep notebooks and code in Git (Databricks Repos integrates directly) and package shared logic as libraries/wheels. A CI pipeline runs unit tests (e.g. pytest over your PySpark logic) and builds the artifact on each pull request; a CD pipeline deploys through the Databricks CLI / REST API or Databricks Asset Bundles, updating job definitions and cluster configs per environment. Secrets come from Key Vault-backed secret scopes, and environment differences (paths, catalog names) are parameterised. So the flow is Git → build/test → deploy jobs — the same discipline as ADF, applied to Spark code.' },
        { q: 'What are the key design principles when designing a data pipeline?',
          a: 'Idempotency first — a rerun produces the same result with no duplicates, so failures are safe to retry. Prefer incremental processing over full reloads to control cost and time. Separate concerns into layers (medallion: raw / clean / curated) so each stage has one responsibility and is independently debuggable. Build in data-quality checks and schema enforcement, and make it observable with logging, lineage and failure alerting. Keep it modular and parameterised so one pipeline serves many tables, and design for scale and cost (right-sized compute, sensible partitioning, auto-termination). In one line: reliable, incremental, layered, observable, and cost-efficient.' },
      ],
    },
  ];

  TV.AzureInterviewQA = TOPICS;
})();
