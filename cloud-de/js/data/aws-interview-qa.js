/* ============================================================
   Cloud DE Visualizer — AWS interview questions, topic-wise.
   Detailed model answers in the same voice as the Azure and
   Databricks banks. Consumed by js/modules/_interview-qa.js.

   Shape: [ { id, label, icon?, blurb?, questions:[ { q, a } ] } ]

   Build progress (20 Q&A / iteration):
     Iter 1  — s3 (10) + glue (10)                       ✅  [20]
     Iter 2  — emr (10) + athena (10)                    ✅  [40]
     Iter 3  — redshift (10) + streaming (10)            ✅  [60]
   ============================================================ */
(function () {
  'use strict';
  const TV = window.TableViz;

  const TOPICS = [
    {
      id: 's3',
      label: 'S3 & Lake Storage',
      icon: 'folder',
      blurb: 'The storage foundation of every AWS pipeline: how S3 behaves as an object store, why that matters for Spark, and how to make lake queries fast and cheap.',
      questions: [
        { q: 'Is S3 a filesystem? Why does that distinction matter for Spark jobs?',
          a: 'No — S3 is a flat object store. Keys like orders/2024/01/file.parquet only look like paths; there are no real directories and no atomic folder rename. Spark commits output by writing to a temp location then renaming it to the final path, and on S3 a "rename" is a copy-then-delete of every object — slow, and historically non-atomic (partial failures could leave garbage). That is exactly why S3-optimized committers (like the magic/directory committers) and manifest-based table formats (Iceberg, Delta, Hudi) exist: they commit by writing a metadata pointer instead of renaming files.' },
        { q: 'How do you make queries over S3 data fast and cheap?',
          a: 'Three levers. First, partition the data by encoding partition columns into the key (dt=2024-01-01/region=EU/) so engines prune to only the relevant prefixes. Second, use a compressed columnar format (Parquet or ORC) so the engine reads only the needed columns, compressed. Third, control file sizes — avoid the small-files problem (thousands of tiny objects kill throughput and metadata performance) by compacting to ~128 MB–1 GB files. On Athena and Spectrum you literally pay per byte scanned, so these directly cut cost as well as latency.' },
        { q: 'What is the small files problem and how do you fix it?',
          a: 'When a job writes thousands of tiny objects (e.g. one file per micro-batch or per task), every read pays per-object overhead: S3 list/GET latency, task scheduling, and metadata lookups dominate over actual data reading, and queries slow dramatically. Fixes: repartition/coalesce before writing to produce fewer, larger files; run periodic compaction jobs (or OPTIMIZE on Delta/Iceberg tables); and for streaming, batch with a larger trigger interval or use a table format that auto-compacts.' },
        { q: 'Walk through the S3 storage classes and when you would use each.',
          a: 'S3 Standard for hot, frequently-accessed working data. Standard-IA (Infrequent Access) for data read occasionally but needing fast retrieval — cheaper storage, small retrieval fee. One Zone-IA for infrequent data you can afford to lose an AZ of (cheaper, single AZ). Glacier Instant/Flexible Retrieval and Glacier Deep Archive for cold history you rarely touch, trading retrieval latency for very low storage cost. Intelligent-Tiering is the safe default when access patterns are unknown — it moves objects between tiers automatically based on usage. Lifecycle rules automate transitions (Standard → IA → Glacier) and expire old data/versions.' },
        { q: 'How would you trigger a pipeline the moment a file lands in S3?',
          a: 'Enable S3 event notifications for the s3:ObjectCreated events on the bucket/prefix and route them to a target: Lambda (run code immediately), SQS (buffer for a consumer), SNS (fan-out), or EventBridge. EventBridge is the most flexible — it can filter on key patterns and fan out to Step Functions, Glue, or a Lambda that starts the ingestion job. This is the event-driven alternative to polling or a fixed schedule.' },
        { q: 'S3 used to be eventually consistent. What is the situation now, and why did it matter?',
          a: 'Since December 2020, S3 provides strong read-after-write consistency for all operations — a new or overwritten object, and list operations, are immediately consistent. Previously, eventual consistency meant a freshly written file might not appear in a listing right away, which broke Spark/Hadoop jobs that write then immediately list output (people used tools like S3Guard/EMRFS consistent view to work around it). Now those workarounds are unnecessary, though the lack of atomic rename (a separate issue) still makes committers and table formats important.' },
        { q: 'How do you secure data in S3?',
          a: 'Layers: block public access at the account/bucket level by default; use IAM policies (identity-based) and bucket policies (resource-based) to grant least-privilege access, with S3 Access Points to give different apps scoped entry points to a shared bucket. Encrypt at rest with SSE-S3 (S3-managed keys) or SSE-KMS (customer-managed KMS keys, with key policies and audit trail); enforce encryption in transit with TLS. For fine-grained analytics access (column/row level), layer Lake Formation on top of the Glue Catalog rather than trying to express that in bucket policies.' },
        { q: 'How does S3 throughput scale, and how does key design affect it?',
          a: 'S3 scales request rate per prefix — roughly 3,500 write and 5,500 read requests per second per prefix — and there is no limit on the number of prefixes. So spreading data across many prefixes (which good partitioning naturally does) increases the parallel throughput readers and writers can achieve. Modern S3 auto-scales and you rarely need the old "random hash prefix" trick, but the principle holds: many prefixes = more parallelism; a single hot prefix can bottleneck.' },
        { q: 'What is S3 versioning and when is it useful in a data platform?',
          a: 'Versioning keeps every version of an object rather than overwriting in place, so you can recover from accidental deletes or overwrites (a delete just adds a delete marker). In a data platform it protects raw/landing data and enables point-in-time recovery, but it also multiplies storage cost, so pair it with lifecycle rules to expire noncurrent versions after N days. Note that table formats like Delta/Iceberg provide their own time-travel/versioning at the table level, which is usually the better mechanism for analytics tables.' },
        { q: 'How would you design an S3 layout for a medallion (Bronze/Silver/Gold) lake?',
          a: 'Typically one bucket per environment (or per domain) with top-level prefixes for each layer: /bronze (raw, immutable landing — often partitioned by ingestion date and source), /silver (cleaned, conformed, deduplicated — partitioned by business date), /gold (curated aggregates/marts for BI). Within each, use Hive-style partitions (dt=…/), store Parquet, and register tables in the Glue Catalog. Keep raw data immutable so you can always reprocess, and separate write permissions per layer (ingestion writes Bronze, ETL writes Silver/Gold) via IAM/Lake Formation.' },
      ],
    },
    {
      id: 'glue',
      label: 'AWS Glue — Catalog & ETL',
      icon: 'arrow-down',
      blurb: 'The metadata backbone and serverless Spark engine of the AWS lakehouse: crawlers, the Data Catalog, DynamicFrames, bookmarks, and how partitions become queryable.',
      questions: [
        { q: 'What is the Glue Data Catalog and what does it actually store?',
          a: 'It is a managed, Hive-metastore-compatible metadata repository. It stores databases, table definitions (columns and types), the data format/SerDe, the S3 location, and the partition list — but never the data itself, which stays on S3. It gives the whole AWS analytics stack (Athena, Redshift Spectrum, EMR, Glue) a single shared source of schema truth, so a table defined once is queryable everywhere without schema drift, and it is the object model Lake Formation attaches permissions to.' },
        { q: 'What does a Glue crawler do, and what are its downsides?',
          a: 'A crawler scans an S3 path (or JDBC source), infers the schema and partitions, and creates or updates the table definition in the catalog automatically. It is convenient for discovering data. Downsides: schema inference can guess wrong types (e.g. everything as string, or an int that should be bigint), it can misinterpret partitions, and running it constantly on huge buckets is slow and costs money. For stable schemas, many teams prefer explicit DDL or defining tables via IaC and skip crawlers, or run them narrowly and on schedule.' },
        { q: 'How do new partitions become visible to Athena/Glue queries?',
          a: 'Several ways: re-run the crawler; run MSCK REPAIR TABLE (scans S3 and adds all missing partitions) or ALTER TABLE ADD PARTITION for a specific one; call the Glue API/batch_create_partition from a job; or use partition projection. Projection is best for high-cardinality or date partitions — you describe the partition pattern in table properties and Athena computes partitions at query time instead of reading them from the catalog, avoiding both metastore bloat and the repair step.' },
        { q: 'What is the difference between a DynamicFrame and a Spark DataFrame in Glue?',
          a: 'A DynamicFrame is Glue’s own abstraction for messy, semi-structured data: it does not need a fixed schema up front and can hold multiple types for a field via a "choice" type, which you reconcile with resolveChoice(). It also has Glue-specific transforms (ApplyMapping, Relationalize) and integrates with bookmarks and the catalog. A Spark DataFrame needs a settled schema and gives you the full Spark SQL API and Catalyst optimizer. In practice you often read dirty input as a DynamicFrame, then toDF() to a DataFrame for the heavy transformations, and convert back if needed for a Glue sink.' },
        { q: 'How do Glue job bookmarks work and why are they useful?',
          a: 'Bookmarks persist state about what a job has already processed — which S3 files (by path/timestamp) or which JDBC key ranges — keyed by a transformation_ctx on each source. On the next run the job reads only new data, giving built-in incremental processing without you tracking watermarks yourself. Requirements: enable bookmarks on the job, give each source a transformation_ctx, and ensure primary keys/sort columns for JDBC. Watch out: bookmarks can behave unexpectedly if you rename sources or reprocess, and you can reset them when you need a full re-run.' },
        { q: 'How is Glue ETL billed, and when would you pick it over EMR?',
          a: 'Glue bills per DPU-hour (a DPU is 4 vCPU + 16 GB), metered in seconds with a short minimum, so you pay only while the job runs. Pick Glue for serverless convenience: short, bursty, or event-driven ETL where you do not want to manage a cluster, and where catalog integration and bookmarks save work. Pick EMR when you need control the serverless service does not give — specific Spark/Hive versions, custom libraries/configs, interactive or long-running clusters, or the lowest cost on very large steady batch via transient Spot clusters. Rule of thumb: Glue for convenience, EMR for control and large-scale cost efficiency.' },
        { q: 'How do you make a Glue job process only new data each run?',
          a: 'Two complementary approaches. Enable job bookmarks with a transformation_ctx on each source so Glue tracks processed files/rows automatically. Or drive incrementality from partitions using a push_down_predicate (e.g. only read dt >= yesterday) so the job reads only the latest partitions from the catalog — this also cuts S3 scan cost because unneeded partitions are never listed or read. For CDC-style sources, combine a bookmark on the raw changes with a MERGE into the target Delta/Iceberg table.' },
        { q: 'What worker types does Glue offer and how do you size a job?',
          a: 'Standard, G.1X (1 DPU = 4 vCPU/16 GB per worker — the common default), G.2X (2 DPU, more memory per worker — for memory-heavy or shuffle-heavy jobs), and G.025X (fractional, for low-volume streaming). Sizing is the main tuning knob: more/bigger workers give more executor cores and memory (more parallelism, fewer spills), but too many waste DPU-seconds on a small job. You right-size by watching for spills/OOM (go bigger, e.g. G.2X) versus under-utilization (fewer workers), and by fixing skew and small-file output rather than just throwing DPUs at it.' },
        { q: 'What is the relationship between the Glue Data Catalog and Lake Formation?',
          a: 'The Data Catalog holds the metadata — what tables exist and their schema. Lake Formation is the governance layer on top: it defines who can access which databases, tables, columns, and rows in that catalog, and enforces those grants across Athena, Spectrum, EMR, and Glue. The catalog answers "what is here"; Lake Formation answers "who is allowed to see it." You register S3 locations with Lake Formation and grant SQL-style permissions (including column and row filters) rather than hand-writing IAM/bucket policies.' },
        { q: 'How would you orchestrate and monitor Glue jobs in a pipeline?',
          a: 'For orchestration: Glue Workflows/triggers for simple Glue-only chains; Step Functions (startJobRun.sync) for AWS-native, event-driven DAGs with built-in retries and branching; or MWAA (Airflow) for complex, code-first, schedule-heavy pipelines using the GlueJobOperator plus sensors. For monitoring: enable job metrics and continuous CloudWatch logs, use the Spark UI/Glue job run insights to diagnose skew, spills, and stage times, set up CloudWatch alarms on failures/duration, and wire failure notifications through SNS. Bookmarks plus idempotent writes make reruns safe.' },
      ],
    },
    {
      id: 'emr',
      label: 'EMR & Big-Data Compute',
      icon: 'zap',
      blurb: 'Managed Spark/Hadoop clusters: node roles, Spot economics, transient clusters, and when EMR beats serverless Glue for large-scale or custom workloads.',
      questions: [
        { q: 'What is EMR and when would you choose it over Glue?',
          a: 'EMR is AWS’s managed platform for running open-source big-data frameworks (Spark, Hive, Presto/Trino, Flink, Hadoop) on a cluster, with S3 as storage via EMRFS. Choose it over Glue when you need control the serverless service does not give: specific Spark/Hive versions, custom libraries or JVM/config tuning, interactive or long-running clusters, or the lowest cost on very large steady batch via transient Spot clusters. Glue wins for short, bursty, event-driven ETL with zero cluster management. Rule of thumb: Glue for serverless convenience, EMR for control and large-scale cost efficiency.' },
        { q: 'Explain the node types in an EMR cluster.',
          a: 'Three roles. The primary (master) node runs the cluster manager (YARN ResourceManager), the driver for some modes, and coordinates work. Core nodes run tasks (executors) AND store data on HDFS plus hold shuffle data — so losing a core node can lose data. Task nodes run compute only (no HDFS), so they are the safe place for Spot instances: if a task node is reclaimed, Spark just retries its tasks elsewhere with no data loss. A common pattern is on-demand core nodes for stability plus Spot task nodes for cheap burst capacity.' },
        { q: 'How does EMR decouple storage from compute, and why does it matter?',
          a: 'EMR uses EMRFS to read and write directly to S3 instead of relying on HDFS on local disk. That means data outlives any single cluster, so you can run transient clusters — spin up, process, terminate — and pay nothing when idle, while multiple clusters and other engines (Athena, Spectrum) share the same S3 data through the Glue Catalog. HDFS on core nodes is then used mainly for intermediate/shuffle data, not the system of record.' },
        { q: 'How do you use Spot instances safely on EMR to cut cost?',
          a: 'Put Spot capacity on task nodes (compute-only), never on the primary and ideally not on core nodes that hold HDFS/shuffle data, so a reclamation only forces task retries rather than data loss. Use instance fleets to mix on-demand and Spot across multiple instance types and AZs to hit a target capacity and survive Spot interruptions in any one pool. Keep a baseline of on-demand core nodes for stability and let Spot task nodes provide the cheap burst. This can cut large-batch compute cost dramatically.' },
        { q: 'What is the difference between a transient and a long-running EMR cluster?',
          a: 'A transient cluster is created for a specific set of steps, runs them, and auto-terminates when done — you pay only for the run, which is ideal and cheapest for scheduled batch ETL. A long-running (persistent) cluster stays up to serve interactive/ad-hoc workloads (notebooks, Presto queries, multiple teams), trading higher idle cost for immediate availability and shared state. Orchestrators (Step Functions/MWAA/Airflow) commonly spin up transient clusters per pipeline run.' },
        { q: 'What are the EMR deployment options — EC2, EKS, Serverless?',
          a: 'EMR on EC2 is the classic model: you manage cluster shape and instances, with maximum control. EMR on EKS runs Spark jobs on a shared Amazon EKS (Kubernetes) cluster, so data teams share infrastructure with other container workloads and get Kubernetes-native isolation. EMR Serverless removes clusters entirely — you submit a Spark/Hive job and AWS auto-provisions and scales the capacity, billing for resources used, similar in spirit to Glue but with the EMR runtime. Choose EC2 for full control, EKS to consolidate on Kubernetes, Serverless for zero infrastructure.' },
        { q: 'How do you submit and orchestrate work on EMR?',
          a: 'Work is submitted as steps (e.g. a spark-submit step) via the console, CLI (add-steps), or SDK; interactive work goes through EMR Notebooks/Studio or a Livy endpoint. For orchestration, Step Functions has native EMR integrations (create cluster, add step with .sync to wait, terminate) and MWAA/Airflow has EMR operators — the typical pattern is: orchestrator creates a transient cluster, runs steps, then terminates it, with retries and failure alerts handled by the orchestrator.' },
        { q: 'How does EMR integrate with the Glue Data Catalog?',
          a: 'You can configure EMR (Spark/Hive/Presto) to use the Glue Data Catalog as its Hive metastore instead of a local metastore. That means EMR sees the same table definitions as Athena and Redshift Spectrum, so a table crawled or defined once is queryable across all engines with no schema drift, and Lake Formation permissions can be enforced. It also makes transient clusters practical — the metastore is external and persistent, so terminating the cluster does not lose table metadata.' },
        { q: 'A Spark job on EMR is running slowly or failing with OOM. How do you approach it?',
          a: 'Standard Spark tuning. First read the Spark UI: look for skew (one task far slower than the rest), heavy shuffle, spills to disk, and GC time. For OOM, check executor memory vs partition size, reduce data per task (more partitions / repartition), fix skew with salting or broadcast joins for small tables, and avoid wide operations that explode memory. Right-size executors (cores/memory) and instance types (memory-optimized if memory-bound). For slow jobs also check S3 read patterns (small files, no partition pruning) and shuffle partition count. Throwing more nodes at skew or small-files won’t help — fix the root cause.' },
        { q: 'How does EMR managed scaling work?',
          a: 'Managed scaling automatically adds and removes core and task nodes within min/max bounds based on YARN metrics (pending containers, resource utilization), so the cluster grows under load and shrinks when idle to save cost. You set the limits and let EMR handle the rest, rather than writing custom scaling policies. It pairs well with Spot task nodes: scale out on Spot for bursts, scale in when the queue drains. For transient batch clusters you often size fixed instead, since the cluster terminates after the run anyway.' },
      ],
    },
    {
      id: 'athena',
      label: 'Athena — Serverless SQL',
      icon: 'cpu',
      blurb: 'Serverless Trino/Presto over S3: the pay-per-scan cost model, partition projection, CTAS, workgroups, and when to reach for a warehouse instead.',
      questions: [
        { q: 'What is Athena and how does it work?',
          a: 'Athena is serverless interactive SQL over data in S3 — no cluster to provision. It runs a managed Trino (formerly Presto) engine, resolves tables through the Glue Data Catalog, reads the underlying Parquet/ORC/CSV/JSON directly from S3, and returns results to an S3 result location. You never load data into it; you point at catalog tables and query in place. It also offers an Athena Spark engine for notebooks and federated queries to other data sources.' },
        { q: 'How is Athena priced, and how do you reduce query cost?',
          a: 'Athena charges per terabyte of data scanned by a query (with a small per-query minimum). You reduce cost by scanning less data: partition tables so WHERE clauses prune to relevant prefixes; store data as compressed columnar Parquet/ORC so only needed columns are read; avoid SELECT *; and use CTAS to convert raw CSV/JSON into partitioned Parquet once. Partition projection helps for date-partitioned tables. Workgroup per-query data-scan limits act as a cost guardrail so a runaway query can’t scan the whole lake.' },
        { q: 'What is partition projection and when do you use it?',
          a: 'Partition projection has Athena compute the list of partitions from a pattern you define in table properties (e.g. a date range with a daily granularity, or an enumerated set), instead of reading partition metadata from the Glue Catalog. You use it for tables with many or high-cardinality partitions (especially date partitions) where the catalog would bloat and MSCK REPAIR would be slow. It removes the need to register partitions at all — Athena derives them at query time — which speeds planning and avoids the "new partitions not visible" problem.' },
        { q: 'What is CTAS in Athena and why is it useful?',
          a: 'CTAS (CREATE TABLE AS SELECT) runs a query and materializes its results as a new table in S3, in a format and layout you choose. It is useful for transforming raw data into optimized Parquet, repartitioning, building derived/aggregate tables without a Spark cluster, and creating smaller working datasets to cut later scan costs. INSERT INTO extends this for incremental appends. For modest data sizes it lets Athena double as a lightweight ELT tool, not just a query engine.' },
        { q: 'When would you use Athena versus Redshift?',
          a: 'Athena is serverless, pay-per-query, and best for ad-hoc/intermittent SQL directly over the S3 lake with no infrastructure to run or pay for when idle. Redshift is a provisioned MPP warehouse (or Serverless) better for high-concurrency BI, complex multi-table joins, and consistently low-latency dashboards over modeled data. A common architecture uses Athena for exploration and lake queries and Redshift as the curated serving layer for high-traffic dashboards, with Redshift Spectrum bridging the two so the warehouse can also reach lake data.' },
        { q: 'What are Athena workgroups?',
          a: 'Workgroups isolate and control query workloads. Each workgroup has its own query result location, and can enforce per-query and per-workgroup data-scan limits (cost control), publish separate CloudWatch metrics, enforce settings like encryption of results, and be tied to IAM policies so different teams/pipelines are separated. They are the primary mechanism for cost governance and multi-tenant separation in Athena — e.g. a "bi" workgroup with a scan cap distinct from an "adhoc" workgroup.' },
        { q: 'Athena depends on which service for table definitions, and what are the implications?',
          a: 'The Glue Data Catalog. Athena has no storage or metastore of its own — it reads table schemas, S3 locations, and partitions from the catalog and the data from S3. Implications: a table must exist in the catalog (via crawler, DDL, or API) before Athena can query it; the same tables are shared with EMR and Redshift Spectrum; and Lake Formation permissions on catalog objects are enforced by Athena. So catalog hygiene (correct types, current partitions) directly determines what and how well Athena can query.' },
        { q: 'How do you handle a query that scans too much data or times out?',
          a: 'Diagnose the scan: is the table partitioned and does the query filter on the partition column (so it prunes)? Is it Parquet/ORC or raw CSV/JSON (columnar cuts scan dramatically)? Is it SELECT * pulling all columns? Fixes: add/adjust partitioning (or partition projection), CTAS the source into compressed partitioned Parquet, select only needed columns, and push filters. For very heavy joins/concurrency that Athena isn’t suited to, move that workload to Redshift. Workgroup scan limits surface offenders early.' },
        { q: 'Can Athena query data outside S3?',
          a: 'Yes — Athena Federated Query uses Lambda-based data source connectors to run SQL against other stores (RDS/JDBC databases, DynamoDB, DocumentDB, Redshift, CloudWatch, and custom sources), and can join that data with S3 tables in a single query. This is useful for enrichment and for querying operational data without a full ETL load, though performance depends on the connector and the remote source, so it’s best for moderate volumes rather than huge scans.' },
        { q: 'What file format and layout choices most affect Athena performance?',
          a: 'Columnar format (Parquet/ORC) is the biggest lever — it lets Athena read only the queried columns and skip row groups via statistics, versus scanning entire rows in CSV/JSON. Compression (Snappy/ZSTD) further cuts bytes scanned. Partitioning on commonly-filtered columns (usually date) enables pruning. Right-sized files (~128 MB–1 GB) avoid both small-file overhead and oversized splits. Together these can reduce data scanned — and therefore cost and latency — by one to two orders of magnitude versus raw uncompressed CSV.' },
      ],
    },
    {
      id: 'redshift',
      label: 'Redshift & Spectrum',
      icon: 'layers',
      blurb: 'The MPP warehouse: distribution/sort keys, RA3 compute-storage separation, COPY/UNLOAD, WLM, and querying the lake in place with Spectrum.',
      questions: [
        { q: 'Explain Redshift’s MPP architecture.',
          a: 'Redshift is a massively parallel processing (MPP), columnar data warehouse. A leader node parses and plans queries and aggregates final results; compute nodes are divided into slices, and data is distributed across those slices so each scans its portion in parallel. Columnar storage plus compression means analytical queries read only the needed columns, compressed. Query performance is largely a function of how well data distribution avoids cross-node data movement (redistribution) during joins and aggregations.' },
        { q: 'What are distribution keys and distribution styles?',
          a: 'The distribution style controls how table rows are spread across node slices. KEY distribution places rows with the same distribution-key value on the same slice — choose the join key so matching rows from two tables are co-located and joins happen locally with no redistribution. ALL replicates a whole (small) table to every node, ideal for small dimension tables so joins never redistribute. EVEN spreads rows round-robin when there’s no good join key. AUTO lets Redshift choose and adapt. A bad DISTKEY causes skew, where a few slices hold most rows and bottleneck every query.' },
        { q: 'What are sort keys and how do they help?',
          a: 'A sort key physically orders the rows in a table on disk. Redshift keeps min/max "zone maps" per block, so when you filter on the sort-key column it can skip entire blocks that can’t match, slashing I/O — great for time-series filtered by date. A compound sort key orders by columns in sequence (best when queries filter on the leading column); an interleaved sort key gives more equal weight to multiple columns but is costlier to maintain. Choosing sort keys to match common filter/range predicates is a core performance lever alongside distribution.' },
        { q: 'What are RA3 nodes and how do they differ from older node types?',
          a: 'RA3 nodes separate compute from storage: data lives in Redshift Managed Storage (RMS), backed by S3, and the node’s local SSD acts as a cache for hot data. That lets you scale compute independently of data volume and pay for storage separately, instead of older DC/DS node types where storage was fixed to the node size (so you had to add compute just to get capacity). RA3 also underpins features like cross-cluster data sharing. Redshift Serverless takes this further by auto-provisioning capacity per workload.' },
        { q: 'How do COPY and UNLOAD work, and why is COPY the right way to load?',
          a: 'COPY bulk-loads data from S3 (or other sources) into Redshift in parallel across all slices, which is dramatically faster than row-by-row INSERTs and is the recommended load path — point it at an S3 prefix of files (ideally split into multiple files so every slice works), specify format (e.g. Parquet) and an IAM role. UNLOAD does the reverse, exporting query results back to S3 as Parquet/CSV in parallel. The pattern is COPY curated data in for fast serving, UNLOAD to share results back to the lake.' },
        { q: 'What is Redshift Spectrum and when do you use it?',
          a: 'Spectrum is a Redshift feature that queries external tables residing in S3 (via an external schema on the Glue Catalog) and joins them to local Redshift tables, using a separate elastic fleet that pushes scans/filters down to S3. You use it to avoid loading cold, huge, or rarely-queried data into the warehouse — keep history in cheap S3 and query it on demand, joining to hot warehouse tables only when needed. It bills per TB scanned in S3 (like Athena), so the same Parquet+partitioning optimizations apply, and it lets you tier data hot-in-Redshift / cold-in-S3.' },
        { q: 'What is Workload Management (WLM) and concurrency scaling?',
          a: 'WLM manages how queries share cluster resources: you define queues (or use Automatic WLM) with memory and concurrency allocations and route queries to them, so, say, ETL loads don’t starve BI dashboards. Query Monitoring Rules can abort or reprioritize runaway queries. Concurrency Scaling adds transient, automatically-managed extra clusters to absorb bursts of concurrent read queries (and some writes), so dashboards stay fast under spikes; you get some free credits and pay per second beyond them. Together they keep mixed workloads predictable.' },
        { q: 'What is the classic Redshift performance trap, and how do you avoid it?',
          a: 'Distribution skew and redistribution. A poorly chosen DISTKEY concentrates rows on a few slices, so those slices become a bottleneck for every query, or forces the optimizer to broadcast/redistribute large tables across the network during joins (visible as DS_BCAST/DS_DIST_BOTH in the plan). Avoid it by co-locating join keys with matching DISTKEY on large fact/dimension pairs, using ALL for small dimensions, and picking sort keys matching filter predicates. Analyze the query plan (EXPLAIN), run ANALYZE/VACUUM to keep stats and sort order fresh, and monitor for skew.' },
        { q: 'How does Redshift connect to and complement the S3 data lake?',
          a: 'Three ways. COPY bulk-loads S3 data into the warehouse in parallel. Spectrum queries data left in S3 directly and joins it to local tables, so you don’t have to load everything. And RA3 managed storage is itself S3-backed. This lets Redshift act as the fast, high-concurrency serving layer for curated/hot data while staying connected to the lake for cold history and bulk data — a lakehouse pattern where recent data lives in Redshift and older data is reached via Spectrum.' },
        { q: 'When would you choose Redshift Serverless over a provisioned cluster?',
          a: 'Choose Serverless when workloads are spiky, intermittent, or hard to size — it auto-provisions capacity (measured in RPUs) per workload and you pay for what you use, with nothing to size or keep warm, ideal for variable BI, dev/test, and unpredictable analytics. Choose a provisioned RA3 cluster for steady, predictable, high-utilization workloads where reserved-instance pricing and fixed capacity are more cost-effective, or when you need specific control. Both share the same engine and can use data sharing to combine.' },
      ],
    },
    {
      id: 'streaming',
      label: 'Streaming — Kinesis & MSK',
      icon: 'activity',
      blurb: 'Real-time ingestion on AWS: Kinesis Data Streams vs Firehose vs Managed Flink, Kafka on MSK, shards, partitions, ordering and delivery to the lake.',
      questions: [
        { q: 'What is the difference between Kinesis Data Streams and Firehose?',
          a: 'Data Streams is a durable, low-latency, sharded stream you consume with your own applications — it supports per-shard ordering, retention/replay (up to 365 days), and multiple independent consumers, but you manage the consumers and scaling. Firehose is a fully-managed delivery service: you point producers at it and it buffers, optionally transforms (via Lambda) and converts to Parquet, and writes to S3/Redshift/OpenSearch/Splunk with no consumers or code to run. Use Streams when you need control, low latency, or replay; use Firehose when you just need streaming data reliably landed in a destination.' },
        { q: 'How do shards, partition keys, and ordering work in Kinesis Data Streams?',
          a: 'A stream is split into shards; each record’s partition key is hashed to decide its shard. Ordering is guaranteed only within a shard, so records that must stay ordered (e.g. all events for one customer) should share a partition key. Each shard has fixed throughput (~1 MB/s or 1,000 records/s in, ~2 MB/s out), so you scale by adding shards (resharding) or using on-demand mode that auto-scales. The trap is a hot key: too many records with the same key overload one shard while others idle, throttling producers/consumers.' },
        { q: 'How would you land a real-time event stream into the data lake with minimal operations?',
          a: 'Use Kinesis Data Firehose. Point producers at a Firehose delivery stream, enable record format conversion to Parquet (using a Glue Catalog schema), set a buffer size/interval to balance file size against latency, optionally attach a Lambda for light transforms, and deliver to a partitioned S3 prefix (with dynamic partitioning on event fields). It requires no consumers, auto-scales, and produces analytics-friendly Parquet in the Bronze layer — the lowest-ops way to get streams into the lake.' },
        { q: 'What is Amazon MSK and when would you choose it over Kinesis?',
          a: 'MSK is managed Apache Kafka — AWS runs the brokers, patching, and recovery while you use the standard Kafka APIs and ecosystem. Choose MSK when you want the open Kafka API and ecosystem (Kafka Connect, Kafka Streams, Schema Registry, existing Kafka apps) or portability across on-prem/other clouds and to avoid vendor lock-in. Choose Kinesis for the least operational overhead and deepest AWS integration when you don’t specifically need Kafka. If a team already runs Kafka, MSK; if they want simplest managed streaming on AWS, Kinesis (especially Firehose for delivery).' },
        { q: 'How do partitions and consumer groups determine throughput and ordering in Kafka/MSK?',
          a: 'A topic is split into partitions; ordering is guaranteed only within a partition, so records needing order share a key that maps to one partition. Within a consumer group, each partition is consumed by exactly one consumer, so the partition count is the ceiling on consumer parallelism — adding consumers beyond the partition count leaves some idle. To increase throughput you add partitions (and consumers up to that count). A skewed key overloads one partition and creates lag on it; monitoring consumer lag tells you whether consumers keep up.' },
        { q: 'What is Kinesis Data Streams retention and replay, and why does it matter?',
          a: 'Records are retained in a stream for a configurable period (default 24 hours, up to 365 days), during which any consumer can re-read them from a chosen position (TRIM_HORIZON for oldest, or a specific sequence number/timestamp). This matters for fault tolerance and reprocessing: if a consumer has a bug or you deploy new logic, you can replay history rather than losing data, and multiple independent consumers can read the same stream at their own pace. Firehose, by contrast, delivers once to a destination and has no replay.' },
        { q: 'How do you process a Kinesis stream in real time?',
          a: 'Options by control/complexity: attach a Lambda as an event-source (it pulls records in batches, great for lightweight enrichment/filtering with auto-scaling to the shard count); use Kinesis Managed Service for Apache Flink for stateful stream processing — windowed aggregations, joins, and complex event processing via Flink or SQL — without operating a Flink cluster; or run the Kinesis Client Library (KCL) in your own app for custom consumers with checkpointing. For Kafka/MSK, the equivalents are Kafka consumers, Kafka Streams, or Flink.' },
        { q: 'What does exactly-once (or at-least-once) mean in these streaming pipelines?',
          a: 'At-least-once means every record is processed one or more times — duplicates are possible on retry, so downstream logic must be idempotent (e.g. MERGE/upsert by key). Exactly-once means each record affects the result once despite failures, achieved through checkpointing plus transactional/idempotent sinks (Flink’s checkpoint + two-phase-commit sinks, or Structured Streaming with a table format like Delta/Iceberg). Kinesis/Lambda is typically at-least-once, so you design idempotent writes; Flink and Spark Structured Streaming can give end-to-end exactly-once with the right sink.' },
        { q: 'How do you get MSK topic data into the S3 data lake?',
          a: 'Common patterns: a Kafka Connect S3 sink connector (config-driven, lands topic data as files with no custom code) — MSK Connect runs managed connectors; or a stream-processing consumer — Spark Structured Streaming or Managed Flink reading the topic and writing partitioned Parquet/Delta/Iceberg to S3. Connect is simplest for raw landing; a processing job is better when you need transformation, exactly-once semantics, or a table format on write. Pair with the Glue Schema Registry for schema governance.' },
        { q: 'How do you scale Kinesis and handle a hot shard / hot partition?',
          a: 'For Kinesis, either switch to on-demand mode (auto-scales shard capacity with traffic) or, in provisioned mode, add shards (split the hot shard) and — most importantly — fix the partition key so load spreads evenly while still preserving the ordering you actually need. A hot shard comes from a skewed key (e.g. keying by a single popular customer/region); choosing a higher-cardinality or composite key distributes records across shards. The same principle applies to Kafka/MSK: increase partitions and choose a well-distributed key to remove the hot partition.' },
      ],
    },
  ];

  TV.AwsInterviewQA = TOPICS;
})();
