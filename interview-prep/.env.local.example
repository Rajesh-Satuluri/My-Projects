# Copy to .env.local and fill in when wiring Phase 1 (Supabase).
# Phase 0 (template) runs entirely on local seed data and needs none of these.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
