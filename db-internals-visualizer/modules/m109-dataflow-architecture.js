export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🌊</div>
    <h3>Dataflow Architecture</h3>
    <p>The dataflow paradigm: operators transform streams, state is explicit, reprocessing is safe.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">DDIA — Full animation coming soon</p>
  </div>`;
  return null;
}
