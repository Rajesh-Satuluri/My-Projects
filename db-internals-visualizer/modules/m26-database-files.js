export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">📁</div>
    <h3>Database Files</h3>
    <p>Heap files, sorted files, hash files — how rows are laid out on disk and why the choice matters for Prime Day writes.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Storage Engine — Full animation coming soon</p>
  </div>`;
  return null;
}
