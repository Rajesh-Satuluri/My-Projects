export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">📑</div>
    <h3>Index Scan</h3>
    <p>Range and point lookups through a B+ tree index — the fast path for most Prime Day product and order queries.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Query Engine — Full animation coming soon</p>
  </div>`;
  return null;
}
