export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🔍</div>
    <h3>Failure Detection</h3>
    <p>Timeouts, Phi Accrual, SWIM — how nodes detect that a peer has crashed without false positives.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Distributed — Full animation coming soon</p>
  </div>`;
  return null;
}
