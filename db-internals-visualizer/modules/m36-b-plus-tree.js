export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🌳</div>
    <h3>B+ Tree</h3>
    <p>Data only at leaves, leaves linked in a doubly-linked list — the index structure inside almost every production RDBMS.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Storage Engine — Full animation coming soon</p>
  </div>`;
  return null;
}
