export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🔍</div>
    <h3>Follow the Query</h3>
    <p>Trace one SQL query from client to result — parser, optimizer, executor, buffer pool, disk.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Labs — Full animation coming soon</p>
  </div>`;
  return null;
}
