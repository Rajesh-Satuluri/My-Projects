export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">▶️</div>
    <h3>Query Execution</h3>
    <p>The Volcano/iterator model: how operators pull rows from each other in a pipeline without materializing intermediate results.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Query Engine — Full animation coming soon</p>
  </div>`;
  return null;
}
