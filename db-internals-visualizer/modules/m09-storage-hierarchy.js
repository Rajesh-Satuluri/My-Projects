export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">💾</div>
    <h3>Storage Hierarchy</h3>
    <p>Registers → L1/L2/L3 cache → DRAM → SSD → HDD → S3: latency numbers every DB engineer must know cold.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Foundations — Full animation coming soon</p>
  </div>`;
  return null;
}
