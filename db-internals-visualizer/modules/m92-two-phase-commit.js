export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">2️⃣</div>
    <h3>Two-Phase Commit</h3>
    <p>Prepare then commit: the classic protocol for cross-shard atomicity and its blocking failure mode.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Distributed — Full animation coming soon</p>
  </div>`;
  return null;
}
