export function mount(container) {
  container.innerHTML = `<div class="coming-soon">
    <div class="coming-soon-icon">🗂️</div>
    <h3>Slotted Pages</h3>
    <p>The slot array + free space management inside a page — how variable-length rows are stored without external fragmentation.</p>
    <p style="margin-top:8px;font-size:11px;opacity:.5">Storage Engine — Full animation coming soon</p>
  </div>`;
  return null;
}
